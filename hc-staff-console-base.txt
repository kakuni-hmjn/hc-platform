
==================================================
staff/index.php
==================================================
<?php

declare(strict_types=1);

require_once __DIR__ . '/lib/bootstrap.php';

$staffDashboard = staff_dashboard_load(
    (int) $staffContext['user']['id']
);

$staffGreeting = staff_greeting();

$pageTitle = 'スタッフコンソール';

require_once __DIR__
    . '/components/layout.php';

staff_layout_start([
    'title' => 'スタッフダッシュボード',
    'heading' => 'ダッシュボード',
    'eyebrow' => 'HC STAFF WORKSPACE',
    'description' => '担当業務、通知、システム状況を確認できます。',
    'active_navigation' => 'dashboard',
    'show_heading' => false,
]);

?>
<section class="staff-page-heading">
                    <div>
                        <p
                            class="staff-page-heading__eyebrow"
                        >
                            HC STAFF WORKSPACE
                        </p>

                        <h2>
                            <?= htmlspecialchars(
                                $staffGreeting['title'],
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>、
                            <?= htmlspecialchars(
                                $staffDisplayName,
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>さん
                        </h2>

                        <p
                            class="staff-page-heading__description"
                        >
                            <?= htmlspecialchars(
                                $staffGreeting['message'],
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>
                            タスク、通知、社内連絡、
                            担当業務をまとめて確認できます。
                        </p>
                    </div>

                    <div
                        class="staff-page-heading__status"
                    >
                        <span></span>

                        <?= htmlspecialchars(
                            $staffRoleName,
                            ENT_QUOTES,
                            'UTF-8'
                        ) ?>
                    </div>
                </section>

                <section class="staff-summary-grid">
                    <a
                        href="/staff/tasks/?status=todo"
                        class="staff-summary-card"
                    >
                        <span>今日のタスク</span>

                        <strong>
                            <?= (int) $staffDashboard[
                                'counts'
                            ]['todo'] ?>
                        </strong>

                        <small>
                            未着手の担当業務
                        </small>
                    </a>

                    <a
                        href="/staff/tasks/?status=in_progress"
                        class="staff-summary-card"
                    >
                        <span>対応中</span>

                        <strong>
                            <?= (int) $staffDashboard[
                                'counts'
                            ]['in_progress'] ?>
                        </strong>

                        <small>
                            現在進行中の仕事
                        </small>
                    </a>

                    <a
                        href="/staff/tasks/?filter=overdue"
                        class="staff-summary-card
                               staff-summary-card--warning"
                    >
                        <span>期限超過</span>

                        <strong>
                            <?= (int) $staffDashboard[
                                'counts'
                            ]['overdue'] ?>
                        </strong>

                        <small>
                            早めの確認が必要
                        </small>
                    </a>

                    <a
                        href="/staff/notifications/"
                        class="staff-summary-card"
                    >
                        <span>未読通知</span>

                        <strong>
                            <?= (int) $staffDashboard[
                                'counts'
                            ]['notifications'] ?>
                        </strong>

                        <small>
                            まだ確認していない通知
                        </small>
                    </a>
                </section>

                <div class="staff-dashboard-grid">
                    <div class="staff-dashboard-column">
                        <section class="staff-panel">
                            <header
                                class="staff-panel__header"
                            >
                                <div>
                                    <h3>自分のタスク</h3>

                                    <p>
                                        優先度と期限順に表示
                                    </p>
                                </div>

                                <a href="/staff/tasks/">
                                    すべて見る
                                </a>
                            </header>

                            <div class="staff-list">
                                <?php if (
                                    $staffDashboard['tasks']
                                    === []
                                ): ?>
                                    <div
                                        class="staff-empty-state"
                                    >
                                        <strong>
                                            担当タスクはありません
                                        </strong>

                                        <p>
                                            新しい仕事が割り当て
                                            られるとここに表示されます。
                                        </p>
                                    </div>
                                <?php else: ?>
                                    <?php foreach (
                                        $staffDashboard['tasks']
                                        as $task
                                    ): ?>
                                        <a
                                            href="/staff/tasks/detail/?id=<?= (int) $task['id'] ?>"
                                            class="staff-list-row"
                                        >
                                            <div>
                                                <strong>
                                                    <?= htmlspecialchars(
                                                        (string) $task[
                                                            'title'
                                                        ],
                                                        ENT_QUOTES,
                                                        'UTF-8'
                                                    ) ?>
                                                </strong>

                                                <p>
                                                    <?= htmlspecialchars(
                                                        (string) $task[
                                                            'task_number'
                                                        ],
                                                        ENT_QUOTES,
                                                        'UTF-8'
                                                    ) ?>

                                                    ·

                                                    <?= htmlspecialchars(
                                                        staff_format_due_date(
                                                            $task[
                                                                'due_at'
                                                            ] ?? null
                                                        ),
                                                        ENT_QUOTES,
                                                        'UTF-8'
                                                    ) ?>
                                                </p>
                                            </div>

                                            <span
                                                class="staff-status
                                                       staff-status--<?= htmlspecialchars(
                                                           (string) $task[
                                                               'status'
                                                           ],
                                                           ENT_QUOTES,
                                                           'UTF-8'
                                                       ) ?>"
                                            >
                                                <?= htmlspecialchars(
                                                    staff_task_status_label(
                                                        (string) $task[
                                                            'status'
                                                        ]
                                                    ),
                                                    ENT_QUOTES,
                                                    'UTF-8'
                                                ) ?>
                                            </span>
                                        </a>
                                    <?php endforeach; ?>
                                <?php endif; ?>
                            </div>
                        </section>

                        <section class="staff-panel">
                            <header
                                class="staff-panel__header"
                            >
                                <div>
                                    <h3>社内連絡</h3>

                                    <p>
                                        最新のお知らせ
                                    </p>
                                </div>

                                <a
                                    href="/staff/announcements/"
                                >
                                    一覧を見る
                                </a>
                            </header>

                            <div class="staff-list">
                                <?php if (
                                    $staffDashboard[
                                        'announcements'
                                    ] === []
                                ): ?>
                                    <div
                                        class="staff-empty-state"
                                    >
                                        <strong>
                                            新しい社内連絡はありません
                                        </strong>
                                    </div>
                                <?php else: ?>
                                    <?php foreach (
                                        $staffDashboard[
                                            'announcements'
                                        ]
                                        as $announcement
                                    ): ?>
                                        <article
                                            class="staff-list-row"
                                        >
                                            <div>
                                                <strong>
                                                    <?= htmlspecialchars(
                                                        (string) $announcement[
                                                            'title'
                                                        ],
                                                        ENT_QUOTES,
                                                        'UTF-8'
                                                    ) ?>
                                                </strong>

                                                <p>
                                                    <?= htmlspecialchars(
                                                        (string) $announcement[
                                                            'body'
                                                        ],
                                                        ENT_QUOTES,
                                                        'UTF-8'
                                                    ) ?>
                                                </p>
                                            </div>

                                            <?php if (
                                                !empty(
                                                    $announcement[
                                                        'requires_confirmation'
                                                    ]
                                                )
                                            ): ?>
                                                <span
                                                    class="staff-status
                                                           staff-status--waiting"
                                                >
                                                    確認必須
                                                </span>
                                            <?php endif; ?>
                                        </article>
                                    <?php endforeach; ?>
                                <?php endif; ?>
                            </div>
                        </section>
                    </div>

                    <aside class="staff-dashboard-side">
                        <section class="staff-panel">
                            <header
                                class="staff-panel__header"
                            >
                                <div>
                                    <h3>担当業務</h3>

                                    <p>
                                        カテゴリと権限から生成
                                    </p>
                                </div>
                            </header>

                            <div class="staff-category-grid">
                                <?php foreach (
                                    $staffContext['categories']
                                    as $category
                                ): ?>
                                    <a
                                        href="/staff/category/?slug=<?= urlencode(
                                            (string) $category['slug']
                                        ) ?>"
                                        class="staff-category-card"
                                    >
                                        <strong>
                                            <?= htmlspecialchars(
                                                (string) $category[
                                                    'name'
                                                ],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </strong>

                                        <small>
                                            <?= htmlspecialchars(
                                                (string) (
                                                    $category[
                                                        'description'
                                                    ] ?? ''
                                                ),
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </small>
                                    </a>
                                <?php endforeach; ?>

                                <?php if (
                                    staff_can_access_admin(
                                        $staffContext
                                    )
                                ): ?>
                                    <a
                                        href="/staff/admin/"
                                        class="staff-category-card"
                                    >
                                        <strong>
                                            全体管理
                                        </strong>

                                        <small>
                                            管理者向け業務機能
                                        </small>
                                    </a>
                                <?php endif; ?>
                            </div>
                        </section>

                        <section class="staff-panel">
                            <header
                                class="staff-panel__header"
                            >
                                <div>
                                    <h3>所属・権限</h3>

                                    <p>
                                        現在有効なスタッフ情報
                                    </p>
                                </div>
                            </header>

                            <div class="staff-context-grid">
                                <div>
                                    <span>基本ロール</span>

                                    <strong>
                                        <?= htmlspecialchars(
                                            $staffRoleName,
                                            ENT_QUOTES,
                                            'UTF-8'
                                        ) ?>
                                    </strong>
                                </div>

                                <div>
                                    <span>カテゴリ</span>

                                    <strong>
                                        <?= count(
                                            $staffContext[
                                                'categories'
                                            ]
                                        ) ?>
                                    </strong>
                                </div>

                                <div>
                                    <span>所属部署</span>

                                    <strong>
                                        <?= count(
                                            $staffContext[
                                                'departments'
                                            ]
                                        ) ?>
                                    </strong>
                                </div>

                                <div>
                                    <span>有効権限</span>

                                    <strong>
                                        <?= count(
                                            $staffContext[
                                                'permissions'
                                            ]
                                        ) ?>
                                    </strong>
                                </div>
                            </div>
                        </section>
                    </aside>
                </div>
<?php

staff_layout_end();

==================================================
staff/lib/bootstrap.php
==================================================
<?php

declare(strict_types=1);

require_once __DIR__ . '/icon.php';
require_once __DIR__ . '/auth.php';
require_once __DIR__ . '/permissions.php';
require_once __DIR__ . '/dashboard.php';
require_once __DIR__ . '/navigation.php';

/*
|--------------------------------------------------------------------------
| HCアカウント認証
|--------------------------------------------------------------------------
*/

$staffAccount = staff_require_account();

$staffAccountId = (int) (
    $staffAccount['id']
    ?? 0
);

/*
|--------------------------------------------------------------------------
| Staff Consoleアクセス権限
|--------------------------------------------------------------------------
*/

if (
    !staff_account_has_permission(
        $staffAccountId,
        'staff.access'
    )
) {
    http_response_code(403);

    exit(
        'このHCアカウントにはスタッフコンソールを利用する権限がありません。'
    );
}

/*
|--------------------------------------------------------------------------
| スタッフ固有プロフィール
|--------------------------------------------------------------------------
*/

$staffUser = staff_find_or_create_user(
    $staffAccount
);

/*
|--------------------------------------------------------------------------
| ロール・権限・部署・カテゴリ
|--------------------------------------------------------------------------
*/

$staffContext = staff_load_context(
    $staffAccount,
    $staffUser
);

staff_require_permission(
    $staffContext,
    'staff.dashboard.view'
);

$staffNavigation = staff_navigation_build(
    $staffContext
);

/*
|--------------------------------------------------------------------------
| 共通表示情報
|--------------------------------------------------------------------------
*/

$staffPrimaryRole = $staffContext['primary_role'];

$staffDisplayName = trim(
    (string) (
        $staffAccount['display_name']
        ?? $staffAccount['username']
        ?? $staffUser['display_name']
        ?? 'Staff'
    )
);

if ($staffDisplayName === '') {
    $staffDisplayName = 'Staff';
}

$staffRoleName = trim(
    (string) (
        $staffPrimaryRole['name']
        ?? 'Staff'
    )
);

if ($staffRoleName === '') {
    $staffRoleName = 'Staff';
}

$staffRoleSlug = trim(
    (string) (
        $staffPrimaryRole['slug']
        ?? 'staff'
    )
);

if ($staffRoleSlug === '') {
    $staffRoleSlug = 'staff';
}

==================================================
staff/lib/navigation.php
==================================================
<?php

declare(strict_types=1);

require_once __DIR__ . '/permissions.php';

function staff_navigation_build(array $context): array
{
    $groups = [
        [
            'label' => 'ワークスペース',
            'items' => [
                [
                    'label' => 'ホーム',
                    'href' => '/staff/',
                    'icon' => 'home',
                    'permission' => 'staff.dashboard.view',
                ],
                [
                    'label' => 'マイタスク',
                    'href' => '/staff/tasks/',
                    'icon' => 'task_alt',
                    'permission' => 'tasks.view.own',
                ],
                [
                    'label' => '通知',
                    'href' => '/staff/notifications/',
                    'icon' => 'notifications',
                    'permission' => 'staff.dashboard.view',
                ],
                [
                    'label' => '社内連絡',
                    'href' => '/staff/announcements/',
                    'icon' => 'campaign',
                    'permission' => 'announcements.view',
                ],
            ],
        ],
        [
            'label' => 'カスタマーサポート',
            'category' => 'support',
            'items' => [
                [
                    'label' => 'お問い合わせ',
                    'href' => '/staff/support/',
                    'icon' => 'support_agent',
                    'permission' => 'support.tickets.view',
                ],
                [
                    'label' => '顧客確認',
                    'href' => '/staff/customers/',
                    'icon' => 'person_search',
                    'permission' => 'support.tickets.view',
                ],
            ],
        ],
        [
            'label' => 'レンタルサーバー事業',
            'category' => 'game-operations',
            'items' => [
                [
                    'label' => '事業ダッシュボード',
                    'href' => '/staff/rental-server/',
                    'icon' => 'space_dashboard',
                    'permission' => 'orders.view',
                ],
                [
                    'label' => '申込・契約',
                    'href' => '/staff/rental-server/contracts/',
                    'icon' => 'receipt_long',
                    'permission' => 'orders.view',
                ],
                [
                    'label' => '作成承認',
                    'href' => '/staff/rental-server/approvals/',
                    'icon' => 'approval',
                    'permission' => 'orders.process',
                ],
                [
                    'label' => 'サーバー一覧',
                    'href' => '/staff/rental-server/servers/',
                    'icon' => 'dns',
                    'permission' => 'orders.view',
                ],
                [
                    'label' => 'ノード状況',
                    'href' => '/staff/rental-server/nodes/',
                    'icon' => 'storage',
                    'permission' => 'infrastructure.servers.view',
                ],
            ],
        ],
        [
            'label' => '開発',
            'category' => 'development',
            'items' => [
                [
                    'label' => 'プロジェクト',
                    'href' => '/staff/development/',
                    'icon' => 'code',
                    'permission' => 'development.projects.view',
                ],
                [
                    'label' => 'デプロイ',
                    'href' => '/staff/deployments/',
                    'icon' => 'rocket_launch',
                    'permission' => 'development.deploy.staging',
                ],
            ],
        ],
        [
            'label' => 'インフラ',
            'category' => 'infrastructure',
            'items' => [
                [
                    'label' => 'システム状態',
                    'href' => '/staff/infrastructure/',
                    'icon' => 'monitor_heart',
                    'permission' => 'infrastructure.servers.view',
                ],
                [
                    'label' => 'サーバー管理',
                    'href' => '/staff/infrastructure/servers/',
                    'icon' => 'storage',
                    'permission' => 'infrastructure.servers.view',
                ],
            ],
        ],
    ];

    if (
        staff_has_permission(
            $context,
            'staff.users.view'
        )
        || staff_has_permission(
            $context,
            'audit.logs.view'
        )
        || staff_can_access_admin($context)
    ) {
        $groups[] = [
            'label' => '管理',
            'items' => [
                [
                    'label' => 'スタッフ管理',
                    'href' => '/staff/admin/users/',
                    'icon' => 'groups',
                    'permission' => 'staff.users.view',
                ],
                [
                    'label' => '有効権限',
                    'href' => '/staff/account/permissions/',
                    'icon' => 'key',
                    'permission' => 'staff.dashboard.view',
                ],
                [
                    'label' => '承認センター',
                    'href' => '/staff/approvals/',
                    'icon' => 'approval',
                    'permission' => 'orders.approve',
                ],
                [
                    'label' => '操作ログ',
                    'href' => '/staff/audit/',
                    'icon' => 'history',
                    'permission' => 'audit.logs.view',
                ],
            ],
        ];
    }

    $visibleGroups = [];

    foreach ($groups as $group) {
        $categorySlug = $group['category'] ?? null;

        if (
            is_string($categorySlug)
            && $categorySlug !== ''
            && !staff_has_category(
                $context,
                $categorySlug
            )
        ) {
            continue;
        }

        $visibleItems = [];

        foreach ($group['items'] as $item) {
            $permission = trim(
                (string) (
                    $item['permission']
                    ?? ''
                )
            );

            if (
                $permission === ''
                || staff_has_permission(
                    $context,
                    $permission
                )
                || staff_can_access_admin($context)
            ) {
                $visibleItems[] = $item;
            }
        }

        if ($visibleItems === []) {
            continue;
        }

        $group['items'] = $visibleItems;
        $visibleGroups[] = $group;
    }

    return $visibleGroups;
}

==================================================
staff/components/layout.php
==================================================
<?php

declare(strict_types=1);

/**
 * /staff/ と同じ共通レイアウトを開始する。
 *
 * 使用例:
 *
 * staff_layout_start([
 *     'title' => 'タスク管理',
 *     'eyebrow' => 'TASK MANAGEMENT',
 *     'description' => '担当業務を確認できます。',
 *     'active_navigation' => 'tasks',
 * ]);
 */
function staff_layout_start(array $options = []): void
{
    global $staffContext;
    global $staffDisplayName;
    global $staffRoleName;
    global $staffRoleSlug;
    global $staffNavigation;

    $title = trim(
        (string) (
            $options['title']
            ?? 'スタッフコンソール'
        )
    );

    $heading = trim(
        (string) (
            $options['heading']
            ?? $title
        )
    );

    $eyebrow = trim(
        (string) (
            $options['eyebrow']
            ?? 'HC STAFF WORKSPACE'
        )
    );

    $description = trim(
        (string) (
            $options['description']
            ?? ''
        )
    );

    $activeNavigation = trim(
        (string) (
            $options['active_navigation']
            ?? ''
        )
    );

    $showHeading = (bool) (
        $options['show_heading']
        ?? true
    );

    /*
     * sidebar.php側で参照できるようにする。
     */
    $staffActiveNavigation = $activeNavigation;

    ?>
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1"
    >

    <meta name="robots" content="noindex,nofollow">

    <title>
        <?= htmlspecialchars(
            $title,
            ENT_QUOTES,
            'UTF-8'
        ) ?>
        | HC Platform
    </title>

    <link
        href="https://fonts.googleapis.com/icon?family=Material+Icons"
        rel="stylesheet"
    >

    <link
        rel="stylesheet"
        href="/staff/staff.css?v=1784394631"
    >
</head>
<body>
    <div class="staff-shell">
        <?php require __DIR__
            . '/../parts/sidebar.php'; ?>

        <div class="staff-workspace">
            <?php require __DIR__
                . '/../parts/topbar.php'; ?>

            <main class="staff-main">
                <?php if ($showHeading): ?>
                    <section class="staff-page-heading">
                        <div>
                            <?php if ($eyebrow !== ''): ?>
                                <p
                                    class="staff-page-heading__eyebrow"
                                >
                                    <?= htmlspecialchars(
                                        $eyebrow,
                                        ENT_QUOTES,
                                        'UTF-8'
                                    ) ?>
                                </p>
                            <?php endif; ?>

                            <h2>
                                <?= htmlspecialchars(
                                    $heading,
                                    ENT_QUOTES,
                                    'UTF-8'
                                ) ?>
                            </h2>

                            <?php if ($description !== ''): ?>
                                <p
                                    class="staff-page-heading__description"
                                >
                                    <?= htmlspecialchars(
                                        $description,
                                        ENT_QUOTES,
                                        'UTF-8'
                                    ) ?>
                                </p>
                            <?php endif; ?>
                        </div>

                        <div
                            class="staff-page-heading__status"
                        >
                            <span></span>

                            <?= htmlspecialchars(
                                (string) $staffRoleName,
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>
                        </div>
                    </section>
                <?php endif; ?>
<?php
}

function staff_layout_end(): void
{
    ?>
            </main>
        </div>
    </div>
    <?php
    require __DIR__
        . '/search-palette.php';
    ?>

    <script src="/staff/staff.js?v=1784390834"></script>
    <script src="/staff/account-menu.js?v=1784391309" defer></script>
    <script src="/staff/search-palette.js?v=1784394631" defer></script>

</body>
</html>
<?php
}

==================================================
staff/parts/topbar.php
==================================================
<?php

declare(strict_types=1);

$staffNotificationCount = (int) (
    $staffDashboard['counts']['notifications']
    ?? 0
);

$staffWorkStatus = trim(
    (string) (
        $staffContext['user']['work_status']
        ?? 'online'
    )
);

$staffWorkStatusLabel = match ($staffWorkStatus) {
    'busy' => '対応中',
    'away' => '離席中',
    'offline' => 'オフライン',
    default => 'オンライン',
};
?>

<header class="staff-topbar">
    <div class="staff-topbar__left">
        <button
            type="button"
            class="staff-mobile-menu"
            data-staff-sidebar-open
            aria-label="メニューを開く"
        >
            <?= staff_icon(
                'menu',
                'staff-mobile-menu__icon',
                24
            ) ?>
        </button>

        <div class="staff-topbar__title">
            <p class="staff-topbar__eyebrow">
                HC PLATFORM
            </p>

            <h1>スタッフコンソール</h1>
        </div>
    </div>

    <div class="staff-topbar__actions">
        
<div class="staff-command-search">
            <button
                type="button"
                class="staff-command-search__trigger"
                data-staff-search-trigger
                aria-label="検索を開く"
                aria-haspopup="dialog"
                aria-expanded="false"
            >
                <span
                    class="material-icons"
                    aria-hidden="true"
                >
                    search
                </span>

                <span class="staff-command-search__placeholder">
                    ページ、タスク、スタッフを検索
                </span>

                <span class="staff-command-search__shortcut">
                    <span data-staff-search-modifier>⌘</span>
                    <span>K</span>
                </span>
            </button>
        </div>


        <div
            class="staff-status-menu"
            data-staff-status-menu
        >
            <button
                type="button"
                class="staff-status-button"
                data-staff-status-trigger
                aria-expanded="false"
                aria-controls="staff-status-dropdown"
                title="<?= htmlspecialchars(
                    $staffWorkStatusLabel,
                    ENT_QUOTES,
                    'UTF-8'
                ) ?>"
            >
                <span
                    class="staff-status-button__dot
                           staff-status-button__dot--<?= htmlspecialchars(
                               $staffWorkStatus,
                               ENT_QUOTES,
                               'UTF-8'
                           ) ?>"
                    data-staff-status-dot
                ></span>

                <span
                    class="staff-status-button__label"
                    data-staff-status-label
                >
                    <?= htmlspecialchars(
                        $staffWorkStatusLabel,
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>
                </span>

                <?= staff_icon(
                    'expand_more',
                    'staff-status-button__arrow',
                    18
                ) ?>
            </button>

            <div
                id="staff-status-dropdown"
                class="staff-status-dropdown"
                data-staff-status-dropdown
                hidden
            >
                <div class="staff-status-dropdown__header">
                    <strong>勤務状態</strong>
                    <span>現在の対応状況を設定</span>
                </div>

                <?php
                $staffStatusOptions = [
                    'online' => [
                        'label' => 'オンライン',
                        'description' => '対応可能です',
                    ],
                    'working' => [
                        'label' => '作業中',
                        'description' => '通常業務を進めています',
                    ],
                    'busy' => [
                        'label' => '対応中',
                        'description' => '別の対応を行っています',
                    ],
                    'away' => [
                        'label' => '離席中',
                        'description' => '一時的に離れています',
                    ],
                    'break' => [
                        'label' => '休憩中',
                        'description' => '休憩しています',
                    ],
                    'offline' => [
                        'label' => 'オフライン',
                        'description' => '現在対応できません',
                    ],
                ];
                ?>

                <div class="staff-status-dropdown__list">
                    <?php foreach (
                        $staffStatusOptions as
                        $statusValue => $statusOption
                    ): ?>
                        <button
                            type="button"
                            class="staff-status-option<?= (
                                $statusValue === $staffWorkStatus
                            ) ? ' is-active' : '' ?>"
                            data-staff-status-option="<?= htmlspecialchars(
                                $statusValue,
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>"
                        >
                            <span
                                class="staff-status-option__dot
                                       staff-status-option__dot--<?= htmlspecialchars(
                                           $statusValue,
                                           ENT_QUOTES,
                                           'UTF-8'
                                       ) ?>"
                            ></span>

                            <span class="staff-status-option__copy">
                                <strong>
                                    <?= htmlspecialchars(
                                        $statusOption['label'],
                                        ENT_QUOTES,
                                        'UTF-8'
                                    ) ?>
                                </strong>

                                <small>
                                    <?= htmlspecialchars(
                                        $statusOption['description'],
                                        ENT_QUOTES,
                                        'UTF-8'
                                    ) ?>
                                </small>
                            </span>

                            <?= staff_icon(
                                'check',
                                'staff-status-option__check',
                                18
                            ) ?>
                        </button>
                    <?php endforeach; ?>
                </div>

                <p
                    class="staff-status-dropdown__message"
                    data-staff-status-message
                    hidden
                ></p>
            </div>
        </div>

        <div
            class="staff-notification-menu"
            data-staff-notifications
        >
            <button
                type="button"
                class="staff-icon-button"
                data-notification-trigger
                aria-label="通知を開く"
                aria-expanded="false"
                aria-controls="staff-notification-panel"
                title="通知"
            >
                <?= staff_icon(
                    $staffNotificationCount > 0
                        ? 'notifications_active'
                        : 'notifications',
                    'notification-bell-icon',
                    22
                ) ?>

                <small
                    data-notification-badge
                    <?= $staffNotificationCount > 0
                        ? ''
                        : 'hidden' ?>
                >
                    <?= min(
                        $staffNotificationCount,
                        99
                    ) ?>
                </small>
            </button>

            <?php require dirname(__DIR__)
                . '/components/notifications-panel.php'; ?>
        </div>

        <?php if (
            staff_can_access_admin(
                $staffContext
            )
        ): ?>
            <a
                href="/admin/"
                class="staff-icon-button"
                aria-label="管理画面"
                title="管理画面"
            >
                <?= staff_icon(
                    'admin_panel_settings',
                    '',
                    22
                ) ?>
            </a>
        <?php endif; ?>

        <div
            class="staff-account-menu"
            data-staff-account-menu
        >
            <button
                type="button"
                class="staff-topbar-profile"
                data-staff-account-trigger
                aria-expanded="false"
                aria-controls="staff-account-dropdown"
                title="アカウントメニュー"
            >
                <span class="staff-topbar-profile__avatar">
                    <?= htmlspecialchars(
                        mb_strtoupper(
                            mb_substr(
                                $staffDisplayName,
                                0,
                                1,
                                'UTF-8'
                            ),
                            'UTF-8'
                        ),
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>

                    <span
                        class="staff-topbar-profile__status
                               staff-topbar-profile__status--<?= htmlspecialchars(
                                   $staffWorkStatus,
                                   ENT_QUOTES,
                                   'UTF-8'
                               ) ?>"
                        data-staff-account-status-dot
                    ></span>
                </span>

                <span class="staff-topbar-profile__copy">
                    <strong>
                        <?= htmlspecialchars(
                            $staffDisplayName,
                            ENT_QUOTES,
                            'UTF-8'
                        ) ?>
                    </strong>

                    <small>
                        <?= htmlspecialchars(
                            $staffRoleName,
                            ENT_QUOTES,
                            'UTF-8'
                        ) ?>
                    </small>
                </span>

                <?= staff_icon(
                    'expand_more',
                    'staff-topbar-profile__arrow',
                    18
                ) ?>
            </button>

            <section
                id="staff-account-dropdown"
                class="staff-account-dropdown"
                data-staff-account-dropdown
                hidden
            >
                <header class="staff-account-dropdown__header">
                    <span class="staff-account-dropdown__avatar">
                        <?= htmlspecialchars(
                            mb_strtoupper(
                                mb_substr(
                                    $staffDisplayName,
                                    0,
                                    1,
                                    'UTF-8'
                                ),
                                'UTF-8'
                            ),
                            ENT_QUOTES,
                            'UTF-8'
                        ) ?>

                        <span
                            class="staff-account-dropdown__status
                                   staff-account-dropdown__status--<?= htmlspecialchars(
                                       $staffWorkStatus,
                                       ENT_QUOTES,
                                       'UTF-8'
                                   ) ?>"
                            data-staff-account-dropdown-status
                        ></span>
                    </span>

                    <div class="staff-account-dropdown__identity">
                        <strong>
                            <?= htmlspecialchars(
                                $staffDisplayName,
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>
                        </strong>

                        <span>
                            <?= htmlspecialchars(
                                $staffRoleName,
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>
                        </span>

                        <small data-staff-account-status-label>
                            <?= htmlspecialchars(
                                $staffWorkStatusLabel,
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>
                        </small>
                    </div>
                </header>

                <nav class="staff-account-dropdown__nav">
                    <a href="/staff/">
                        <?= staff_icon('dashboard', '', 19) ?>
                        <span>スタッフダッシュボード</span>
                    </a>

                    <a href="/dashboard/">
                        <?= staff_icon('account_circle', '', 19) ?>
                        <span>HCアカウント</span>
                    </a>

                    <a href="/staff/tasks/">
                        <?= staff_icon('task_alt', '', 19) ?>
                        <span>マイタスク</span>
                    </a>

                    <a href="/staff/notifications/">
                        <?= staff_icon('notifications', '', 19) ?>
                        <span>通知一覧</span>
                    </a>
                </nav>

                <?php if (
                    staff_can_access_admin(
                        $staffContext
                    )
                ): ?>
                    <div class="staff-account-dropdown__divider"></div>

                    <nav class="staff-account-dropdown__nav">
                        <a href="/admin/">
                            <?= staff_icon(
                                'admin_panel_settings',
                                '',
                                19
                            ) ?>
                            <span>管理画面</span>
                        </a>
                    </nav>
                <?php endif; ?>

                <div class="staff-account-dropdown__divider"></div>

                <nav class="staff-account-dropdown__nav">
                    <a
                        href="/logout/"
                        class="staff-account-dropdown__logout"
                    >
                        <?= staff_icon('logout', '', 19) ?>
                        <span>ログアウト</span>
                    </a>
                </nav>
            </section>
        </div>
    </div>
</header>

==================================================
staff/parts/sidebar.php
==================================================
<?php

declare(strict_types=1);
$currentPath = parse_url(
    $_SERVER['REQUEST_URI'] ?? '/staff/',
    PHP_URL_PATH
);

$currentPath = is_string($currentPath)
    ? rtrim($currentPath, '/') . '/'
    : '/staff/';

function staff_sidebar_initial(string $name): string
{
    $name = trim($name);

    if ($name === '') {
        return 'S';
    }

    return mb_strtoupper(
        mb_substr($name, 0, 1, 'UTF-8'),
        'UTF-8'
    );
}
?>

<aside
    class="staff-sidebar"
    data-staff-sidebar
>
    <div class="staff-sidebar__brand">
        <a
            href="/staff/"
            class="staff-sidebar__brand-link"
            aria-label="スタッフコンソール ホーム"
        >
            <div class="staff-brand-mark">
                HC
            </div>

            <div class="staff-brand-copy">
                <strong>Staff Console</strong>
                <span>HC Platform</span>
            </div>
        </a>

        <button
            type="button"
            class="staff-sidebar__close"
            data-staff-sidebar-close
            aria-label="メニューを閉じる"
        >
            <?= staff_icon(
                'close',
                'staff-sidebar__close-icon',
                22
            ) ?>
        </button>
    </div>

    <nav
        class="staff-sidebar__nav"
        aria-label="スタッフメニュー"
    >
        <?php foreach (
            $staffNavigation as $group
        ): ?>
            <section class="staff-nav-group">
                <p class="staff-nav-group__label">
                    <?= htmlspecialchars(
                        (string) $group['label'],
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>
                </p>

                <div class="staff-nav-group__items">
                    <?php foreach (
                        $group['items'] as $item
                    ): ?>
                        <?php
                        $itemPath = rtrim(
                            (string) $item['href'],
                            '/'
                        ) . '/';

                        $isActive =
                            $currentPath === $itemPath
                            || (
                                $itemPath !== '/staff/'
                                && str_starts_with(
                                    $currentPath,
                                    $itemPath
                                )
                            );
                        ?>

                        <a
                            href="<?= htmlspecialchars(
                                (string) $item['href'],
                                ENT_QUOTES,
                                'UTF-8'
                            ) ?>"
                            class="staff-nav-item<?= $isActive
                                ? ' staff-nav-item--active'
                                : '' ?>"
                            <?= $isActive
                                ? 'aria-current="page"'
                                : '' ?>
                        >
                            <span
                                class="staff-nav-item__icon"
                            >
                                <?= staff_icon(
                                    (string) (
                                        $item['icon']
                                        ?? 'circle'
                                    ),
                                    'staff-nav-icon',
                                    19
                                ) ?>
                            </span>

                            <span
                                class="staff-nav-item__label"
                            >
                                <?= htmlspecialchars(
                                    (string) $item['label'],
                                    ENT_QUOTES,
                                    'UTF-8'
                                ) ?>
                            </span>

                            <?php if ($isActive): ?>
                                <?= staff_icon(
                                    'chevron_right',
                                    'staff-nav-item__arrow',
                                    18
                                ) ?>
                            <?php endif; ?>
                        </a>
                    <?php endforeach; ?>
                </div>
            </section>
        <?php endforeach; ?>
    </nav>

    <div class="staff-sidebar__account">
        <div class="staff-account-avatar">
            <?= htmlspecialchars(
                staff_sidebar_initial(
                    $staffDisplayName
                ),
                ENT_QUOTES,
                'UTF-8'
            ) ?>
        </div>

        <div class="staff-sidebar__account-copy">
            <strong>
                <?= htmlspecialchars(
                    $staffDisplayName,
                    ENT_QUOTES,
                    'UTF-8'
                ) ?>
            </strong>

            <span>
                <?= htmlspecialchars(
                    $staffRoleName,
                    ENT_QUOTES,
                    'UTF-8'
                ) ?>
            </span>
        </div>

        <a
            href="/staff/account/"
            class="staff-sidebar__account-action"
            aria-label="スタッフアカウント設定"
            title="アカウント設定"
        >
            <?= staff_icon(
                'manage_accounts',
                '',
                19
            ) ?>
        </a>
    </div>
</aside>

<div
    class="staff-sidebar-backdrop"
    data-staff-sidebar-backdrop
></div>

==================================================
staff/staff.css
==================================================
:root {
    color-scheme: light;

    --staff-sidebar-width: 244px;
    --staff-topbar-height: 72px;

    --staff-bg: #f3f6fa;
    --staff-surface: #ffffff;
    --staff-surface-soft: #f7f9fc;
    --staff-border: #dde4ed;

    --staff-text: #111827;
    --staff-muted: #667085;

    --staff-sidebar: #09111f;
    --staff-sidebar-active: #1a3978;

    --staff-primary: #2563eb;
    --staff-green: #0d9b72;
    --staff-red: #eb4d52;
    --staff-orange: #d88725;
}

* {
    box-sizing: border-box;
}

html {
    min-height: 100%;
    background: var(--staff-bg);
}

body {
    min-width: 320px;
    min-height: 100vh;
    margin: 0;
    color: var(--staff-text);
    background: var(--staff-bg);
    font-family:
        Inter,
        "Noto Sans JP",
        "Hiragino Kaku Gothic ProN",
        "Yu Gothic",
        sans-serif;
    -webkit-font-smoothing: antialiased;
}

a {
    color: inherit;
    text-decoration: none;
}

button {
    font: inherit;
}

.staff-shell {
    min-height: 100vh;
}

.staff-sidebar {
    position: fixed;
    z-index: 100;
    inset: 0 auto 0 0;

    display: flex;
    width: var(--staff-sidebar-width);
    flex-direction: column;

    color: #edf3ff;
    background:
        radial-gradient(
            circle at 20% 0,
            rgba(37, 78, 160, 0.28),
            transparent 31%
        ),
        var(--staff-sidebar);
}

.staff-sidebar__brand {
    display: flex;
    min-height: var(--staff-topbar-height);
    align-items: center;
    gap: 12px;
    padding: 0 17px;

    border-bottom: 1px solid
        rgba(255, 255, 255, 0.06);
}

.staff-brand-mark {
    display: flex;
    width: 38px;
    height: 38px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;

    border-radius: 12px;
    color: #101827;
    background: #fff;

    font-size: 12px;
    font-weight: 900;
}

.staff-brand-copy {
    display: flex;
    flex-direction: column;
    gap: 3px;
}

.staff-brand-copy strong {
    font-size: 14px;
}

.staff-brand-copy span {
    color: #8190a8;
    font-size: 10px;
}

.staff-sidebar__close {
    display: none;
    margin-left: auto;
    padding: 0;

    border: 0;
    color: #fff;
    background: transparent;

    font-size: 27px;
    cursor: pointer;
}

.staff-sidebar__nav {
    flex: 1;
    padding: 17px 11px;
    overflow-y: auto;
}

.staff-nav-group + .staff-nav-group {
    margin-top: 22px;
}

.staff-nav-group__label {
    margin: 0 9px 8px;
    color: #66768f;
    font-size: 9px;
    font-weight: 800;
    letter-spacing: 0.08em;
}

.staff-nav-group__items {
    display: grid;
    gap: 4px;
}

.staff-nav-item {
    display: flex;
    min-height: 40px;
    align-items: center;
    gap: 10px;
    padding: 7px 10px;

    border-radius: 10px;
    color: #aeb9ca;

    font-size: 11px;
    font-weight: 700;
}

.staff-nav-item:hover {
    color: #fff;
    background: rgba(255, 255, 255, 0.055);
}

.staff-nav-item--active {
    color: #fff;
    background: var(--staff-sidebar-active);
}

.staff-nav-item__icon {
    display: flex;
    width: 24px;
    height: 24px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;

    border-radius: 8px;
    color: #9aaac0;
    background: rgba(255, 255, 255, 0.07);

    font-size: 9px;
    font-weight: 900;
}

.staff-nav-item--active .staff-nav-item__icon {
    color: #fff;
    background: var(--staff-green);
}

.staff-nav-item__label {
    min-width: 0;
    flex: 1;
}

.staff-sidebar__account {
    display: flex;
    align-items: center;
    gap: 11px;
    margin: 10px;
    padding: 11px;

    border: 1px solid
        rgba(255, 255, 255, 0.04);
    border-radius: 13px;

    background: rgba(255, 255, 255, 0.07);
}

.staff-account-avatar {
    display: flex;
    width: 34px;
    height: 34px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;

    border-radius: 10px;
    color: #fff;
    background: var(--staff-green);

    font-size: 11px;
    font-weight: 900;
}

.staff-sidebar__account-copy {
    display: flex;
    min-width: 0;
    flex: 1;
    flex-direction: column;
    gap: 3px;
}

.staff-sidebar__account-copy strong {
    overflow: hidden;
    font-size: 11px;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-sidebar__account-copy span {
    color: #8996a9;
    font-size: 9px;
}

.staff-account-status {
    width: 8px;
    height: 8px;
    flex: 0 0 auto;

    border: 2px solid #192333;
    border-radius: 50%;
    background: #25c488;
}

.staff-workspace {
    min-height: 100vh;
    margin-left: var(--staff-sidebar-width);
}

.staff-topbar {
    position: sticky;
    z-index: 50;
    top: 0;

    display: flex;
    min-height: var(--staff-topbar-height);
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    padding: 0 26px;

    border-bottom: 1px solid var(--staff-border);
    background: rgba(255, 255, 255, 0.96);
    backdrop-filter: blur(16px);
}

.staff-topbar__left {
    display: flex;
    align-items: center;
    gap: 14px;
}

.staff-topbar__eyebrow {
    margin: 0 0 4px;
    color: #667590;
    font-size: 9px;
    font-weight: 900;
    letter-spacing: 0.14em;
}

.staff-topbar h1 {
    margin: 0;
    font-size: 17px;
    letter-spacing: -0.025em;
}

.staff-topbar__actions {
    display: flex;
    align-items: center;
    gap: 8px;
}

.staff-topbar-button,
.staff-icon-button {
    display: inline-flex;
    min-height: 36px;
    align-items: center;
    justify-content: center;

    border: 1px solid var(--staff-border);
    border-radius: 10px;
    color: #111827;
    background: #fff;

    font-size: 10px;
    font-weight: 800;
}

.staff-topbar-button {
    padding: 0 14px;
}

.staff-icon-button {
    position: relative;
    width: 36px;
}

.staff-icon-button small {
    position: absolute;
    top: -5px;
    right: -5px;

    display: flex;
    width: 17px;
    height: 17px;
    align-items: center;
    justify-content: center;

    border: 2px solid #fff;
    border-radius: 50%;
    color: #fff;
    background: var(--staff-red);

    font-size: 8px;
}

.staff-mobile-menu {
    display: none;
    width: 38px;
    height: 38px;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 4px;

    border: 1px solid var(--staff-border);
    border-radius: 10px;
    background: #fff;
}

.staff-mobile-menu span {
    width: 16px;
    height: 2px;
    background: #1f2937;
}

.staff-main {
    width: min(100%, 1450px);
    margin: 0 auto;
    padding: 32px 26px 60px;
}

.staff-page-heading {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 23px;
}

.staff-page-heading__eyebrow {
    margin: 0 0 13px;
    color: var(--staff-green);
    font-size: 9px;
    font-weight: 900;
    letter-spacing: 0.14em;
}

.staff-page-heading h2 {
    margin: 0;
    font-size: clamp(28px, 4vw, 40px);
    line-height: 1.08;
    letter-spacing: -0.055em;
}

.staff-page-heading__description {
    max-width: 720px;
    margin: 17px 0 0;
    color: var(--staff-muted);
    font-size: 11px;
    line-height: 1.8;
}

.staff-page-heading__status {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;

    border: 1px solid #d7e5de;
    border-radius: 999px;
    color: #34765d;
    background: #f2faf6;

    font-size: 9px;
    font-weight: 800;
}

.staff-page-heading__status span {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #24b984;
}

.staff-summary-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 12px;
    margin-bottom: 15px;
}

.staff-summary-card {
    min-height: 112px;
    padding: 18px;

    border: 1px solid var(--staff-border);
    border-radius: 15px;
    background: var(--staff-surface);
}

.staff-summary-card:hover {
    border-color: #b6c4d5;
    transform: translateY(-1px);
}

.staff-summary-card--warning {
    border-color: #edc7c5;
    background:
        linear-gradient(
            135deg,
            rgba(235, 77, 82, 0.05),
            transparent
        ),
        #fff;
}

.staff-summary-card span {
    display: block;
    color: #59677c;
    font-size: 9px;
    font-weight: 800;
}

.staff-summary-card strong {
    display: block;
    margin-top: 14px;
    font-size: 28px;
    line-height: 1;
}

.staff-summary-card small {
    display: block;
    margin-top: 9px;
    color: var(--staff-muted);
    font-size: 8px;
}

.staff-dashboard-grid {
    display: grid;
    grid-template-columns:
        minmax(0, 1.7fr)
        minmax(310px, 0.75fr);
    align-items: start;
    gap: 15px;
}

.staff-dashboard-column,
.staff-dashboard-side {
    display: grid;
    gap: 15px;
}

.staff-panel {
    overflow: hidden;
    border: 1px solid var(--staff-border);
    border-radius: 16px;
    background: var(--staff-surface);
}

.staff-panel__header {
    display: flex;
    min-height: 70px;
    align-items: center;
    justify-content: space-between;
    gap: 15px;
    padding: 15px 17px;

    border-bottom: 1px solid var(--staff-border);
}

.staff-panel__header h3 {
    margin: 0;
    font-size: 14px;
}

.staff-panel__header p {
    margin: 5px 0 0;
    color: var(--staff-muted);
    font-size: 9px;
}

.staff-panel__header a {
    color: var(--staff-green);
    font-size: 9px;
    font-weight: 800;
}

.staff-list {
    min-height: 210px;
}

.staff-list-row {
    display: flex;
    min-height: 67px;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 14px 17px;

    border-bottom: 1px solid #edf1f6;
}

.staff-list-row:last-child {
    border-bottom: 0;
}

.staff-list-row:hover {
    background: #fafcff;
}

.staff-list-row strong {
    display: block;
    font-size: 11px;
}

.staff-list-row p {
    margin: 6px 0 0;
    color: var(--staff-muted);
    font-size: 8px;
    line-height: 1.6;
}

.staff-status {
    display: inline-flex;
    min-width: 48px;
    height: 26px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;
    padding: 0 9px;

    border-radius: 999px;
    color: #58677c;
    background: #eef2f7;

    font-size: 8px;
    font-weight: 900;
}

.staff-status--in_progress {
    color: #1c66c6;
    background: #edf5ff;
}

.staff-status--review,
.staff-status--waiting {
    color: #a26918;
    background: #fff4df;
}

.staff-empty-state {
    display: flex;
    min-height: 210px;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    padding: 30px;
    text-align: center;
}

.staff-empty-state strong {
    font-size: 12px;
}

.staff-empty-state p {
    max-width: 330px;
    margin: 8px 0 0;
    color: var(--staff-muted);
    font-size: 9px;
    line-height: 1.7;
}

.staff-category-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 8px;
    padding: 13px;
}

.staff-category-card {
    min-height: 105px;
    padding: 14px;

    border: 1px solid var(--staff-border);
    border-radius: 12px;
    background: var(--staff-surface-soft);
}

.staff-category-card:hover {
    border-color: #afc0d5;
    background: #fff;
}

.staff-category-card strong {
    display: block;
    font-size: 10px;
}

.staff-category-card small {
    display: block;
    margin-top: 9px;
    color: var(--staff-muted);
    font-size: 8px;
    line-height: 1.6;
}

.staff-context-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1px;
    padding: 1px;
    background: var(--staff-border);
}

.staff-context-grid > div {
    min-height: 82px;
    padding: 16px;
    background: #fff;
}

.staff-context-grid span {
    display: block;
    color: var(--staff-muted);
    font-size: 8px;
}

.staff-context-grid strong {
    display: block;
    margin-top: 8px;
    font-size: 16px;
}

.staff-sidebar-backdrop {
    display: none;
}

@media (max-width: 1100px) {
    .staff-summary-grid {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }

    .staff-dashboard-grid {
        grid-template-columns: 1fr;
    }

    .staff-dashboard-side {
        grid-template-columns:
            repeat(2, minmax(0, 1fr));
    }
}

@media (max-width: 800px) {
    .staff-sidebar {
        transform: translateX(-100%);
        transition: transform 180ms ease;
    }

    .staff-sidebar--open {
        transform: translateX(0);
    }

    .staff-sidebar__close {
        display: block;
    }

    .staff-sidebar-backdrop {
        position: fixed;
        z-index: 90;
        inset: 0;
        background: rgba(5, 11, 20, 0.58);
    }

    .staff-sidebar-backdrop--visible {
        display: block;
    }

    .staff-workspace {
        margin-left: 0;
    }

    .staff-mobile-menu {
        display: flex;
    }

    .staff-main {
        padding: 25px 16px 45px;
    }

    .staff-topbar {
        padding: 0 16px;
    }
}

@media (max-width: 620px) {
    .staff-topbar-button--secondary,
    .staff-icon-button {
        display: none;
    }

    .staff-page-heading {
        align-items: flex-start;
        flex-direction: column;
    }

    .staff-summary-grid,
    .staff-dashboard-side {
        grid-template-columns: 1fr;
    }

    .staff-category-grid {
        grid-template-columns: 1fr;
    }
}

/* ==================================================
   Material Icons
================================================== */

.material-icons {
    display: inline-flex;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;

    width: 1em;
    height: 1em;

    font-family: "Material Icons";
    font-weight: normal;
    font-style: normal;
    line-height: 1;
    letter-spacing: normal;
    text-transform: none;
    white-space: nowrap;
    word-wrap: normal;

    direction: ltr;
    user-select: none;

    -webkit-font-feature-settings: "liga";
    -webkit-font-smoothing: antialiased;
    font-feature-settings: "liga";
}

/* ==================================================
   Sidebar icons
================================================== */

.staff-sidebar__brand-link {
    display: flex;
    min-width: 0;
    flex: 1;
    align-items: center;
    gap: 12px;
}

.staff-sidebar__close {
    display: none;
    width: 36px;
    height: 36px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;
    margin-left: auto;
    padding: 0;

    border: 0;
    border-radius: 9px;
    color: #9eabc0;
    background: transparent;

    cursor: pointer;
}

.staff-sidebar__close:hover {
    color: #fff;
    background: rgba(255, 255, 255, 0.08);
}

.staff-sidebar__close-icon {
    pointer-events: none;
}

.staff-nav-item__icon {
    display: flex;
    width: 30px;
    height: 30px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;

    border-radius: 9px;
    color: #8291a8;
    background: transparent;
}

.staff-nav-icon {
    pointer-events: none;
}

.staff-nav-item:hover .staff-nav-item__icon {
    color: #dbe7f7;
}

.staff-nav-item--active .staff-nav-item__icon {
    color: #fff;
    background: rgba(26, 157, 112, 0.95);
}

.staff-nav-item__arrow {
    margin-left: auto;
    color: #7fa6de;
}

.staff-sidebar__account-action {
    display: flex;
    width: 34px;
    height: 34px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;

    border-radius: 9px;
    color: #8190a6;
}

.staff-sidebar__account-action:hover {
    color: #fff;
    background: rgba(255, 255, 255, 0.07);
}

/* ==================================================
   Topbar
================================================== */

.staff-topbar__title {
    min-width: 0;
}

.staff-topbar__actions {
    display: flex;
    align-items: center;
    gap: 8px;
}

.staff-topbar-search {
    width: min(300px, 28vw);
}

.staff-topbar-search__field {
    display: flex;
    height: 38px;
    align-items: center;
    gap: 8px;
    padding: 0 12px;

    border: 1px solid var(--staff-border);
    border-radius: 10px;
    color: var(--staff-muted);
    background: var(--staff-surface-soft);
}

.staff-topbar-search__field:focus-within {
    border-color: rgba(37, 99, 235, 0.42);
    background: #fff;
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.07);
}

.staff-topbar-search__icon {
    flex: 0 0 auto;
    color: #8a97aa;
}

.staff-topbar-search input {
    width: 100%;
    min-width: 0;
    padding: 0;

    border: 0;
    outline: 0;
    color: var(--staff-text);
    background: transparent;

    font: inherit;
    font-size: 10px;
}

.staff-topbar-search input::placeholder {
    color: #9ba6b5;
}

.staff-icon-button {
    position: relative;

    display: inline-flex;
    width: 38px;
    height: 38px;
    min-height: 38px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;
    padding: 0;

    border: 1px solid var(--staff-border);
    border-radius: 10px;
    color: #4f5d70;
    background: #fff;
}

.staff-icon-button:hover {
    border-color: #bcc7d5;
    color: #111827;
    background: #f8fafc;
}

.notification-bell-icon {
    pointer-events: none;
}

.staff-icon-button small {
    position: absolute;
    top: -5px;
    right: -5px;

    display: flex;
    min-width: 18px;
    height: 18px;
    align-items: center;
    justify-content: center;
    padding: 0 4px;

    border: 2px solid #fff;
    border-radius: 999px;
    color: #fff;
    background: var(--staff-red);

    font-size: 8px;
    font-weight: 900;
    line-height: 1;
}

.staff-status-button {
    display: flex;
    height: 38px;
    align-items: center;
    gap: 7px;
    padding: 0 10px;

    border: 1px solid var(--staff-border);
    border-radius: 10px;
    color: #4d5b6e;
    background: #fff;

    cursor: pointer;
}

.staff-status-button:hover {
    border-color: #bcc7d5;
    background: #f8fafc;
}

.staff-status-button__dot {
    width: 8px;
    height: 8px;
    flex: 0 0 auto;
    border-radius: 50%;
    background: #24b984;
}

.staff-status-button__dot--busy {
    background: #e49a2f;
}

.staff-status-button__dot--away {
    background: #76859a;
}

.staff-status-button__dot--offline {
    background: #aeb7c3;
}

.staff-status-button__label {
    font-size: 9px;
    font-weight: 800;
}

.staff-status-button__arrow {
    color: #8e9aaa;
}

.staff-topbar-profile {
    display: flex;
    min-width: 150px;
    height: 42px;
    align-items: center;
    gap: 9px;
    padding: 4px 9px 4px 5px;

    border: 1px solid var(--staff-border);
    border-radius: 12px;
    background: #fff;
}

.staff-topbar-profile:hover {
    border-color: #bcc7d5;
    background: #f8fafc;
}

.staff-topbar-profile__avatar {
    display: flex;
    width: 32px;
    height: 32px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;

    border-radius: 9px;
    color: #fff;
    background: var(--staff-green);

    font-size: 10px;
    font-weight: 900;
}

.staff-topbar-profile__copy {
    display: flex;
    min-width: 0;
    flex: 1;
    flex-direction: column;
    gap: 2px;
}

.staff-topbar-profile__copy strong {
    overflow: hidden;
    color: var(--staff-text);
    font-size: 9px;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-topbar-profile__copy small {
    overflow: hidden;
    color: var(--staff-muted);
    font-size: 8px;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-topbar-profile__arrow {
    color: #8e9aaa;
}

.staff-mobile-menu {
    display: none;
    width: 38px;
    height: 38px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: center;
    padding: 0;

    border: 1px solid var(--staff-border);
    border-radius: 10px;
    color: #29364a;
    background: #fff;
}

.staff-mobile-menu__icon {
    pointer-events: none;
}

@media (max-width: 1100px) {
    .staff-topbar-search {
        display: none;
    }

    .staff-topbar-profile {
        min-width: 0;
    }

    .staff-topbar-profile__copy,
    .staff-topbar-profile__arrow {
        display: none;
    }
}

@media (max-width: 800px) {
    .staff-sidebar__close {
        display: flex;
    }

    .staff-mobile-menu {
        display: flex;
    }
}

@media (max-width: 620px) {
    .staff-status-button {
        display: none;
    }

    .staff-topbar-profile {
        height: 38px;
        padding: 2px;
        border: 0;
        background: transparent;
    }

    .staff-topbar-profile__avatar {
        width: 34px;
        height: 34px;
    }
}

/* =========================================================
   Staff UI readability improvements
   ========================================================= */

:root {
    --staff-text-primary: #f8fafc;
    --staff-text-secondary: #cbd5e1;
    --staff-text-muted: #94a3b8;
    --staff-border-strong: rgba(255, 255, 255, 0.12);
    --staff-panel-readable: rgba(20, 28, 43, 0.96);
}

html {
    font-size: 16px;
}

body {
    font-family:
        Inter,
        "Noto Sans JP",
        "Hiragino Sans",
        "Yu Gothic UI",
        "Yu Gothic",
        sans-serif;
    color: var(--staff-text-primary);
    line-height: 1.7;
    -webkit-font-smoothing: antialiased;
    text-rendering: optimizeLegibility;
}

.staff-main {
    color: var(--staff-text-primary);
}

.staff-page-heading__eyebrow {
    color: #8eb8ff;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.12em;
}

.staff-page-heading h1,
.staff-page-heading h2 {
    color: var(--staff-text-primary);
    font-weight: 800;
    line-height: 1.35;
    letter-spacing: -0.02em;
}

.staff-page-heading__description {
    color: var(--staff-text-secondary);
    font-size: 0.96rem;
    line-height: 1.8;
}

.staff-page-heading__status {
    color: var(--staff-text-primary);
    font-weight: 700;
}

.staff-summary-card {
    border-color: var(--staff-border-strong);
}

.staff-summary-card > span {
    color: var(--staff-text-secondary);
    font-size: 0.92rem;
    font-weight: 700;
}

.staff-summary-card > strong {
    color: var(--staff-text-primary);
    font-size: clamp(2rem, 4vw, 2.65rem);
    font-weight: 800;
    line-height: 1.15;
}

.staff-summary-card > small {
    color: var(--staff-text-muted);
    font-size: 0.84rem;
    line-height: 1.6;
}

.staff-panel {
    border-color: var(--staff-border-strong);
}

.staff-panel__header h3 {
    color: var(--staff-text-primary);
    font-size: 1.08rem;
    font-weight: 800;
    line-height: 1.4;
}

.staff-panel__header p {
    color: var(--staff-text-muted);
    font-size: 0.86rem;
    line-height: 1.6;
}

.staff-panel__header a {
    color: #8eb8ff;
    font-size: 0.88rem;
    font-weight: 700;
}

.staff-list-row {
    color: var(--staff-text-primary);
    border-color: rgba(255, 255, 255, 0.08);
}

.staff-list-row > div > strong,
.staff-list-row strong {
    color: var(--staff-text-primary);
    font-size: 0.96rem;
    font-weight: 750;
    line-height: 1.5;
}

.staff-list-row p {
    color: var(--staff-text-secondary);
    font-size: 0.84rem;
    line-height: 1.65;
}

.staff-empty-state strong {
    color: var(--staff-text-primary);
    font-size: 1rem;
    font-weight: 750;
}

.staff-empty-state p {
    color: var(--staff-text-secondary);
    font-size: 0.9rem;
    line-height: 1.7;
}

.staff-sidebar__brand strong,
.staff-brand-copy strong {
    color: var(--staff-text-primary);
    font-weight: 800;
}

.staff-sidebar__brand span,
.staff-brand-copy span {
    color: var(--staff-text-muted);
}

.staff-nav-group__label {
    color: var(--staff-text-muted);
    font-size: 0.73rem;
    font-weight: 800;
    letter-spacing: 0.1em;
}

.staff-nav-item,
.staff-nav-link,
.staff-nav-group__items a {
    color: var(--staff-text-secondary);
    font-size: 0.93rem;
    font-weight: 650;
}

.staff-nav-item:hover,
.staff-nav-link:hover,
.staff-nav-group__items a:hover {
    color: var(--staff-text-primary);
}

.staff-nav-item.is-active,
.staff-nav-link.is-active,
.staff-nav-group__items a.is-active {
    color: #ffffff;
    font-weight: 800;
}

.staff-topbar {
    color: var(--staff-text-primary);
}

.staff-topbar strong,
.staff-topbar__title {
    color: var(--staff-text-primary);
    font-weight: 750;
}

.staff-topbar span,
.staff-topbar p,
.staff-topbar small {
    color: var(--staff-text-secondary);
}

.staff-status {
    font-weight: 750;
    letter-spacing: 0.01em;
}

input,
select,
textarea,
button {
    font: inherit;
}

input,
select,
textarea {
    color: var(--staff-text-primary);
}

input::placeholder,
textarea::placeholder {
    color: var(--staff-text-muted);
    opacity: 1;
}

@media (max-width: 768px) {
    html {
        font-size: 15px;
    }

    .staff-page-heading__description {
        font-size: 0.92rem;
    }

    .staff-list-row p {
        font-size: 0.82rem;
    }
}

/* Light theme readability */
[data-theme="light"] {
    --staff-text-primary: #172033;
    --staff-text-secondary: #475569;
    --staff-text-muted: #64748b;
    --staff-border-strong: rgba(15, 23, 42, 0.13);
}

[data-theme="light"] body,
body[data-theme="light"] {
    color: var(--staff-text-primary);
}


/* =========================================================
   HC Staff Console — dark workspace fix
   ========================================================= */

html,
body {
    background: #07101f !important;
    color: #f8fafc !important;
}

.staff-shell,
.staff-workspace,
.staff-main {
    background: #07101f !important;
    color: #f8fafc !important;
}

.staff-workspace {
    min-height: 100vh;
}

/* 上部ヘッダー */
.staff-topbar {
    background: #0b1424 !important;
    border-bottom: 1px solid rgba(148, 163, 184, 0.15) !important;
    color: #f8fafc !important;
}

.staff-topbar *,
.staff-topbar strong,
.staff-topbar span,
.staff-topbar p,
.staff-topbar small,
.staff-topbar button {
    color: inherit;
}

/* 検索ボックス */
.staff-topbar input,
.staff-topbar__search,
.staff-search {
    background: #111c2e !important;
    border-color: rgba(148, 163, 184, 0.2) !important;
    color: #f8fafc !important;
}

.staff-topbar input::placeholder {
    color: #7f8da3 !important;
}

/* ページ見出し */
.staff-page-heading {
    background: transparent !important;
    border: 0 !important;
    color: #f8fafc !important;
}

.staff-page-heading h1,
.staff-page-heading h2 {
    color: #f8fafc !important;
}

.staff-page-heading__eyebrow {
    color: #74a7ff !important;
}

.staff-page-heading__description {
    color: #aebbd0 !important;
}

.staff-page-heading__status {
    background: rgba(16, 185, 129, 0.08) !important;
    border-color: rgba(16, 185, 129, 0.22) !important;
    color: #d8f9eb !important;
}

/* サマリーカード */
.staff-summary-card {
    background: #0e192a !important;
    border: 1px solid rgba(148, 163, 184, 0.14) !important;
    color: #f8fafc !important;
    box-shadow: none !important;
}

.staff-summary-card:hover {
    background: #122035 !important;
    border-color: rgba(96, 165, 250, 0.35) !important;
}

.staff-summary-card > span {
    color: #b7c3d5 !important;
}

.staff-summary-card > strong {
    color: #ffffff !important;
}

.staff-summary-card > small {
    color: #8796ad !important;
}

.staff-summary-card--warning {
    background:
        linear-gradient(
            135deg,
            rgba(245, 158, 11, 0.1),
            rgba(14, 25, 42, 1) 55%
        ) !important;
}

/* パネル */
.staff-panel {
    background: #0e192a !important;
    border: 1px solid rgba(148, 163, 184, 0.14) !important;
    color: #f8fafc !important;
    box-shadow: none !important;
}

.staff-panel__header {
    background: #0e192a !important;
    border-bottom: 1px solid rgba(148, 163, 184, 0.13) !important;
}

.staff-panel__header h3 {
    color: #f8fafc !important;
}

.staff-panel__header p {
    color: #8f9db2 !important;
}

.staff-panel__header a {
    color: #6ea8ff !important;
}

/* リスト */
.staff-list,
.staff-list-row {
    background: transparent !important;
}

.staff-list-row {
    border-color: rgba(148, 163, 184, 0.1) !important;
    color: #f8fafc !important;
}

a.staff-list-row:hover {
    background: rgba(96, 165, 250, 0.06) !important;
}

.staff-list-row strong {
    color: #edf4ff !important;
}

.staff-list-row p {
    color: #93a2b8 !important;
}

/* 空表示 */
.staff-empty-state {
    background: transparent !important;
}

.staff-empty-state strong {
    color: #eaf1fb !important;
}

.staff-empty-state p {
    color: #91a0b6 !important;
}

/* 権限・所属などの小カード */
.staff-panel table,
.staff-panel td,
.staff-panel th {
    background: transparent !important;
    border-color: rgba(148, 163, 184, 0.13) !important;
    color: #e7eef9 !important;
}

/* ボタン・セレクト */
.staff-topbar button,
.staff-topbar select,
.staff-topbar__action,
.staff-topbar__account {
    background: #111c2e !important;
    border-color: rgba(148, 163, 184, 0.18) !important;
    color: #e7eef9 !important;
}

/* モーダルやドロップダウン */
.staff-dropdown,
.staff-menu,
.staff-popover,
.staff-modal,
.staff-notification-panel {
    background: #0e192a !important;
    border-color: rgba(148, 163, 184, 0.16) !important;
    color: #f8fafc !important;
}

/* 誤ってライトテーマが保存されていてもスタッフ画面はダーク表示 */
html[data-theme="light"],
body[data-theme="light"],
[data-theme="light"] .staff-shell,
[data-theme="light"] .staff-workspace,
[data-theme="light"] .staff-main {
    background: #07101f !important;
    color: #f8fafc !important;
}

[data-theme="light"] .staff-topbar {
    background: #0b1424 !important;
}

[data-theme="light"] .staff-summary-card,
[data-theme="light"] .staff-panel,
[data-theme="light"] .staff-panel__header {
    background: #0e192a !important;
    color: #f8fafc !important;
}

[data-theme="light"] .staff-summary-card strong,
[data-theme="light"] .staff-panel h3,
[data-theme="light"] .staff-list-row strong {
    color: #f8fafc !important;
}

[data-theme="light"] .staff-summary-card span,
[data-theme="light"] .staff-summary-card small,
[data-theme="light"] .staff-panel p,
[data-theme="light"] .staff-list-row p {
    color: #93a2b8 !important;
}


/* ==========================================================
   Readability only (背景は変更しない)
   ========================================================== */

body {
    color: #f5f7fa;
    line-height: 1.7;
    -webkit-font-smoothing: antialiased;
}

.staff-page-heading h1,
.staff-page-heading h2 {
    color: #ffffff;
    font-size: 2.3rem;
    font-weight: 800;
    letter-spacing: -.02em;
}

.staff-page-heading__eyebrow {
    color: #78a8ff;
    font-weight: 800;
    letter-spacing: .12em;
}

.staff-page-heading__description {
    color: #c5d0df;
    font-size: .98rem;
}

.staff-page-heading__status {
    color: #ffffff;
    font-weight: 700;
}

.staff-summary-card span {
    color: #b8c6d8;
    font-weight: 700;
}

.staff-summary-card strong {
    color: #ffffff;
    font-size: 2.4rem;
    font-weight: 800;
}

.staff-summary-card small {
    color: #94a7bd;
}

.staff-panel__header h3 {
    color: #ffffff;
    font-size: 1.1rem;
    font-weight: 800;
}

.staff-panel__header p {
    color: #9fb0c5;
}

.staff-panel__header a {
    color: #7fb2ff;
    font-weight: 700;
}

.staff-list-row strong {
    color: #ffffff;
    font-size: .97rem;
    font-weight: 700;
}

.staff-list-row p {
    color: #a7b7cb;
    line-height: 1.6;
}

.staff-empty-state strong {
    color: #ffffff;
}

.staff-empty-state p {
    color: #9fb0c5;
}

.staff-nav-group__label {
    color: #7f90a6;
    font-size: .72rem;
    font-weight: 800;
    letter-spacing: .12em;
}

.staff-nav-group__items a {
    color: #d7e1ee;
    font-size: .95rem;
    font-weight: 600;
}

.staff-nav-group__items a:hover {
    color: #ffffff;
}

.staff-nav-group__items a.is-active {
    color: #ffffff;
    font-weight: 800;
}

.staff-brand-copy strong {
    color: #ffffff;
}

.staff-brand-copy span {
    color: #9fb0c5;
}

.staff-topbar strong {
    color: #ffffff;
}

.staff-topbar span,
.staff-topbar small,
.staff-topbar p {
    color: #a8b8ca;
}

button,
.staff-topbar button {
    font-weight: 600;
}

.staff-status {
    font-weight: 700;
    letter-spacing: .02em;
}

table th {
    color: #ffffff;
    font-weight: 700;
}

table td {
    color: #d5dfec;
}


/* ==========================================================
   HC Workspace Readability
   背景は変更しない
   ========================================================== */

body{
    color:#1e293b;
}

.staff-main{
    color:#1e293b;
}

/* ページタイトル */

.staff-page-heading h1,
.staff-page-heading h2{
    color:#111827;
    font-size:2.35rem;
    font-weight:800;
    line-height:1.25;
}

.staff-page-heading__eyebrow{
    color:#2563eb;
    font-weight:800;
    letter-spacing:.12em;
}

.staff-page-heading__description{
    color:#475569;
    font-size:1rem;
    line-height:1.8;
}

.staff-page-heading__status{
    color:#111827;
    font-weight:700;
}

/* サマリー */

.staff-summary-card span{
    color:#64748b;
    font-weight:700;
}

.staff-summary-card strong{
    color:#111827;
    font-size:2.5rem;
    font-weight:800;
}

.staff-summary-card small{
    color:#64748b;
}

/* パネル */

.staff-panel__header h3{
    color:#111827;
    font-size:1.1rem;
    font-weight:800;
}

.staff-panel__header p{
    color:#64748b;
}

.staff-panel__header a{
    color:#2563eb;
    font-weight:700;
}

/* リスト */

.staff-list-row strong{
    color:#111827;
    font-weight:700;
}

.staff-list-row p{
    color:#64748b;
}

/* Empty */

.staff-empty-state strong{
    color:#111827;
}

.staff-empty-state p{
    color:#64748b;
}

/* Topbar */

.staff-topbar strong{
    color:#111827;
}

.staff-topbar span,
.staff-topbar small,
.staff-topbar p{
    color:#64748b;
}

/* サイドバーは現在の配色を維持 */

.staff-sidebar{
    color:#fff;
}

.staff-brand-copy strong{
    color:#fff;
}

.staff-brand-copy span{
    color:#cbd5e1;
}

.staff-nav-group__label{
    color:#94a3b8;
}

.staff-nav-group__items a{
    color:#dbe4ef;
    font-weight:600;
}

.staff-nav-group__items a:hover{
    color:#fff;
}

.staff-nav-group__items a.is-active{
    color:#fff;
    font-weight:700;
}


/* ==========================================================
   Final staff light workspace override
   白背景 / 濃い文字 / サイドバーのみ濃紺
   ========================================================== */

html,
body,
.staff-shell,
.staff-workspace,
.staff-main {
    background: #f4f7fb !important;
    color: #172033 !important;
}

/* ヘッダー */
.staff-topbar {
    background: #ffffff !important;
    color: #172033 !important;
    border-bottom-color: #dbe3ee !important;
}

.staff-topbar strong,
.staff-topbar__title {
    color: #172033 !important;
}

.staff-topbar span,
.staff-topbar p,
.staff-topbar small {
    color: #64748b !important;
}

.staff-topbar input,
.staff-topbar select,
.staff-topbar button,
.staff-topbar__search,
.staff-topbar__action,
.staff-topbar__account {
    background: #ffffff !important;
    color: #334155 !important;
    border-color: #d8e1ec !important;
}

.staff-topbar input::placeholder {
    color: #94a3b8 !important;
}

/* ページタイトル部分 */
.staff-page-heading {
    background: transparent !important;
    color: #172033 !important;
    border: 0 !important;
}

.staff-page-heading h1,
.staff-page-heading h2 {
    color: #111827 !important;
}

.staff-page-heading__eyebrow {
    color: #3977df !important;
}

.staff-page-heading__description {
    color: #64748b !important;
}

.staff-page-heading__status {
    color: #166534 !important;
    background: #f0fdf4 !important;
    border-color: #bbf7d0 !important;
}

/* サマリーカード */
.staff-summary-card,
[data-theme="light"] .staff-summary-card {
    background: #ffffff !important;
    color: #172033 !important;
    border-color: #e1e7ef !important;
}

.staff-summary-card > span {
    color: #64748b !important;
}

.staff-summary-card > strong {
    color: #111827 !important;
}

.staff-summary-card > small {
    color: #64748b !important;
}

.staff-summary-card--warning {
    background:
        linear-gradient(
            135deg,
            #fff7ed 0%,
            #ffffff 70%
        ) !important;
}

/* パネル */
.staff-panel,
.staff-panel__header,
[data-theme="light"] .staff-panel,
[data-theme="light"] .staff-panel__header {
    background: #ffffff !important;
    color: #172033 !important;
    border-color: #e1e7ef !important;
}

.staff-panel__header h3 {
    color: #111827 !important;
}

.staff-panel__header p {
    color: #64748b !important;
}

.staff-panel__header a {
    color: #2563eb !important;
}

/* 一覧 */
.staff-list,
.staff-list-row {
    background: transparent !important;
}

.staff-list-row {
    color: #172033 !important;
    border-color: #e6ebf2 !important;
}

.staff-list-row strong {
    color: #111827 !important;
}

.staff-list-row p {
    color: #64748b !important;
}

a.staff-list-row:hover {
    background: #f8fafc !important;
}

/* 空表示 */
.staff-empty-state {
    background: transparent !important;
}

.staff-empty-state strong {
    color: #111827 !important;
}

.staff-empty-state p {
    color: #64748b !important;
}

/* テーブル */
.staff-panel table,
.staff-panel th,
.staff-panel td {
    background: #ffffff !important;
    border-color: #e1e7ef !important;
}

.staff-panel th {
    color: #475569 !important;
}

.staff-panel td {
    color: #172033 !important;
}

/* サイドバーは既存の濃紺を維持 */
.staff-sidebar {
    background: #071426 !important;
    color: #ffffff !important;
}

.staff-sidebar .staff-brand-copy strong,
.staff-sidebar .staff-sidebar__account strong,
.staff-sidebar .staff-nav-group__items a {
    color: #dce6f3 !important;
}

.staff-sidebar .staff-brand-copy span,
.staff-sidebar .staff-sidebar__account span,
.staff-sidebar .staff-nav-group__label {
    color: #8fa1b8 !important;
}

.staff-sidebar .staff-nav-group__items a:hover,
.staff-sidebar .staff-nav-group__items a.is-active {
    color: #ffffff !important;
}

/* モーダル・メニュー */
.staff-dropdown,
.staff-menu,
.staff-popover,
.staff-modal,
.staff-notification-panel {
    background: #ffffff !important;
    color: #172033 !important;
    border-color: #dbe3ee !important;
}


/* ==========================================================
   Light Theme Hover & Transition
   ========================================================== */

/* 共通アニメーション */
.staff-summary-card,
.staff-panel,
.staff-list-row,
.staff-nav-group__items a,
.staff-topbar button,
.staff-topbar__action {
    transition:
        background-color .18s ease,
        border-color .18s ease,
        color .18s ease,
        transform .18s ease,
        box-shadow .18s ease;
}

/* サマリーカード */
.staff-summary-card:hover{
    background:#f8fafc !important;
    border-color:#60a5fa !important;
    box-shadow:0 8px 22px rgba(37,99,235,.10);
    transform:translateY(-2px);
}

/* パネル */
.staff-panel:hover{
    border-color:#d4dde8 !important;
    box-shadow:0 8px 24px rgba(15,23,42,.06);
}

/* リスト */
.staff-list-row:hover{
    background:#f8fafc !important;
    color:#111827 !important;
}

.staff-list-row:hover strong{
    color:#111827 !important;
}

.staff-list-row:hover p{
    color:#475569 !important;
}

/* ヘッダーリンク */
.staff-panel__header a:hover{
    color:#1d4ed8 !important;
}

/* Topbarボタン */
.staff-topbar button:hover,
.staff-topbar__action:hover{
    background:#eff6ff !important;
    border-color:#93c5fd !important;
    color:#1d4ed8 !important;
}

/* 検索 */
.staff-topbar input:focus{
    border-color:#60a5fa !important;
    box-shadow:0 0 0 4px rgba(59,130,246,.12);
}

/* サイドバーは今の雰囲気維持 */
.staff-nav-group__items a:hover{
    background:rgba(255,255,255,.08) !important;
}

.staff-nav-group__items a.is-active{
    background:rgba(59,130,246,.18) !important;
}

/* カード内リンク */
.staff-summary-card:hover span{
    color:#475569 !important;
}

.staff-summary-card:hover strong{
    color:#111827 !important;
}

.staff-summary-card:hover small{
    color:#64748b !important;
}


/* ==========================================================
   Staff Notification Center
   ========================================================== */

.staff-notifications {
    position: relative;
    flex: 0 0 auto;
}

.staff-notifications__trigger {
    position: relative;
    width: 42px;
    height: 42px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    border: 1px solid #dbe3ee !important;
    border-radius: 12px;
    background: #ffffff !important;
    color: #475569 !important;
    cursor: pointer;
}

.staff-notifications__trigger:hover,
.staff-notifications__trigger[aria-expanded="true"] {
    background: #eff6ff !important;
    border-color: #93c5fd !important;
    color: #2563eb !important;
}

.staff-notifications__trigger:focus-visible {
    outline: none;
    box-shadow: 0 0 0 4px rgba(59, 130, 246, .14);
}

.staff-notifications__bell {
    width: 21px;
    height: 21px;
}

.staff-notifications__badge {
    position: absolute;
    top: -6px;
    right: -6px;
    min-width: 20px;
    height: 20px;
    padding: 0 5px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 2px solid #ffffff;
    border-radius: 999px;
    background: #dc2626;
    color: #ffffff;
    font-size: 10px;
    font-weight: 800;
    line-height: 1;
}

.staff-notifications__panel {
    position: absolute;
    top: calc(100% + 12px);
    right: 0;
    z-index: 1000;
    width: min(430px, calc(100vw - 28px));
    overflow: hidden;
    border: 1px solid #dbe3ee !important;
    border-radius: 18px;
    background: #ffffff !important;
    color: #172033 !important;
    box-shadow:
        0 24px 70px rgba(15, 23, 42, .18),
        0 8px 24px rgba(15, 23, 42, .08);
}

.staff-notifications__panel[hidden] {
    display: none !important;
}

.staff-notifications__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 18px 18px 14px;
    background: #ffffff !important;
    border-bottom: 1px solid #e5eaf1;
}

.staff-notifications__header h2 {
    margin: 2px 0 0;
    color: #111827 !important;
    font-size: 18px;
    font-weight: 800;
}

.staff-notifications__eyebrow {
    display: block;
    color: #64748b !important;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: .12em;
    text-transform: uppercase;
}

.staff-notifications__read-all {
    padding: 8px 10px;
    border: 0 !important;
    border-radius: 9px;
    background: transparent !important;
    color: #2563eb !important;
    font-size: 12px;
    font-weight: 700;
    cursor: pointer;
}

.staff-notifications__read-all:hover {
    background: #eff6ff !important;
    color: #1d4ed8 !important;
}

.staff-notifications__read-all:disabled {
    color: #94a3b8 !important;
    cursor: not-allowed;
}

.staff-notifications__filters {
    display: flex;
    gap: 6px;
    padding: 11px 14px;
    overflow-x: auto;
    border-bottom: 1px solid #e5eaf1;
    background: #ffffff !important;
    scrollbar-width: none;
}

.staff-notifications__filters::-webkit-scrollbar {
    display: none;
}

.staff-notifications__filters button {
    flex: 0 0 auto;
    padding: 7px 10px;
    border: 1px solid transparent !important;
    border-radius: 999px;
    background: transparent !important;
    color: #64748b !important;
    font-size: 11px;
    font-weight: 700;
    cursor: pointer;
}

.staff-notifications__filters button:hover {
    background: #f1f5f9 !important;
    color: #334155 !important;
}

.staff-notifications__filters button.is-active {
    background: #eff6ff !important;
    border-color: #bfdbfe !important;
    color: #1d4ed8 !important;
}

.staff-notifications__body {
    max-height: 480px;
    overflow-y: auto;
    background: #ffffff !important;
}

.staff-notifications__item {
    position: relative;
    display: grid;
    grid-template-columns: 42px minmax(0, 1fr);
    gap: 12px;
    width: 100%;
    padding: 15px 17px;
    border: 0 !important;
    border-bottom: 1px solid #edf0f5 !important;
    background: #ffffff !important;
    color: #172033 !important;
    text-align: left;
    text-decoration: none;
    cursor: pointer;
}

.staff-notifications__item:hover {
    background: #f8fafc !important;
}

.staff-notifications__item.is-unread {
    background: #f7faff !important;
}

.staff-notifications__item.is-unread:hover {
    background: #eff6ff !important;
}

.staff-notifications__item.is-unread::after {
    content: "";
    position: absolute;
    top: 18px;
    right: 15px;
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #2563eb;
}

.staff-notifications__icon {
    width: 42px;
    height: 42px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 12px;
    font-size: 17px;
    font-weight: 800;
}

.staff-notifications__icon--critical {
    background: #fef2f2;
    color: #dc2626;
}

.staff-notifications__icon--warning {
    background: #fff7ed;
    color: #ea580c;
}

.staff-notifications__icon--success {
    background: #f0fdf4;
    color: #16a34a;
}

.staff-notifications__icon--info {
    background: #eff6ff;
    color: #2563eb;
}

.staff-notifications__icon--development {
    background: #faf5ff;
    color: #9333ea;
}

.staff-notifications__content {
    min-width: 0;
    padding-right: 13px;
}

.staff-notifications__item-title {
    display: block;
    margin: 0 0 4px;
    overflow: hidden;
    color: #111827 !important;
    font-size: 13px;
    font-weight: 800;
    line-height: 1.45;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-notifications__message {
    display: -webkit-box;
    margin: 0;
    overflow: hidden;
    color: #64748b !important;
    font-size: 12px;
    line-height: 1.55;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
}

.staff-notifications__meta {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 7px;
    color: #94a3b8 !important;
    font-size: 10px;
    font-weight: 600;
}

.staff-notifications__category {
    padding: 3px 6px;
    border-radius: 999px;
    background: #f1f5f9;
    color: #64748b !important;
}

.staff-notifications__loading,
.staff-notifications__empty,
.staff-notifications__error {
    padding: 44px 24px;
    color: #64748b !important;
    font-size: 13px;
    text-align: center;
}

.staff-notifications__error {
    color: #b91c1c !important;
}

.staff-notifications__footer {
    display: flex;
    justify-content: center;
    padding: 11px 16px;
    border-top: 1px solid #e5eaf1;
    background: #f8fafc !important;
    color: #64748b !important;
    font-size: 11px;
    font-weight: 600;
}

@media (max-width: 640px) {
    .staff-notifications__panel {
        position: fixed;
        top: 72px;
        right: 12px;
        left: 12px;
        width: auto;
        max-height: calc(100vh - 90px);
    }

    .staff-notifications__body {
        max-height: calc(100vh - 260px);
    }
}


/* ==========================================================
   Existing topbar notification bell
   ========================================================== */

.staff-notification-menu {
    position: relative;
    display: inline-flex;
    align-items: center;
}

.staff-notification-menu > .staff-icon-button {
    position: relative;
    appearance: none;
    font: inherit;
    cursor: pointer;
}

.staff-notification-menu > .staff-icon-button small {
    position: absolute;
    top: -5px;
    right: -5px;
    min-width: 18px;
    height: 18px;
    padding: 0 5px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 2px solid #ffffff;
    border-radius: 999px;
    background: #dc2626 !important;
    color: #ffffff !important;
    font-size: 9px;
    font-weight: 800;
    line-height: 1;
}

.staff-notification-menu > .staff-icon-button small[hidden] {
    display: none !important;
}

.staff-notification-menu
> .staff-icon-button[aria-expanded="true"] {
    background: #eff6ff !important;
    border-color: #93c5fd !important;
    color: #2563eb !important;
}

.staff-notification-menu .staff-notifications__panel {
    top: calc(100% + 12px);
    right: 0;
}


/* ==========================================================
   Notification open action
   ========================================================== */

.staff-notifications__meta {
    flex-wrap: wrap;
}

.staff-notifications__open {
    margin-left: auto;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 4px 7px;
    border-radius: 7px;
    color: #2563eb !important;
    font-size: 10px;
    font-weight: 800;
    line-height: 1;
    cursor: pointer;
    user-select: none;
}

.staff-notifications__open:hover {
    background: #dbeafe !important;
    color: #1d4ed8 !important;
}

.staff-notifications__open:focus-visible {
    outline: 2px solid #60a5fa;
    outline-offset: 2px;
}

.staff-notifications__item:not(.is-unread) {
    opacity: .82;
}

.staff-notifications__item:not(.is-unread):hover {
    opacity: 1;
}


/* ==========================================================
   Staff work status menu
   ========================================================== */

.staff-status-menu {
    position: relative;
    display: inline-flex;
    align-items: center;
}

.staff-status-dropdown {
    position: absolute;
    top: calc(100% + 12px);
    right: 0;
    z-index: 1100;
    width: 310px;
    overflow: hidden;
    border: 1px solid #dbe3ee;
    border-radius: 16px;
    background: #ffffff;
    box-shadow:
        0 24px 70px rgba(15, 23, 42, .16),
        0 8px 24px rgba(15, 23, 42, .08);
}

.staff-status-dropdown[hidden] {
    display: none !important;
}

.staff-status-dropdown__header {
    display: flex;
    flex-direction: column;
    gap: 3px;
    padding: 16px;
    border-bottom: 1px solid #e5eaf1;
}

.staff-status-dropdown__header strong {
    color: #111827;
    font-size: 14px;
    font-weight: 800;
}

.staff-status-dropdown__header span {
    color: #64748b;
    font-size: 11px;
}

.staff-status-dropdown__list {
    padding: 7px;
}

.staff-status-option {
    width: 100%;
    display: grid;
    grid-template-columns: 12px minmax(0, 1fr) 20px;
    align-items: center;
    gap: 11px;
    padding: 11px;
    border: 0;
    border-radius: 11px;
    background: #ffffff;
    color: #172033;
    text-align: left;
    cursor: pointer;
}

.staff-status-option:hover {
    background: #f8fafc !important;
    color: #172033 !important;
}

.staff-status-option.is-active {
    background: #eff6ff !important;
}

.staff-status-option:disabled {
    cursor: wait;
    opacity: .65;
}

.staff-status-option__dot,
.staff-status-button__dot {
    flex: 0 0 auto;
    width: 9px;
    height: 9px;
    border-radius: 999px;
    background: #94a3b8;
}

.staff-status-option__dot--online,
.staff-status-button__dot--online {
    background: #22c55e;
}

.staff-status-option__dot--working,
.staff-status-button__dot--working {
    background: #3b82f6;
}

.staff-status-option__dot--busy,
.staff-status-button__dot--busy {
    background: #ef4444;
}

.staff-status-option__dot--away,
.staff-status-button__dot--away {
    background: #eab308;
}

.staff-status-option__dot--break,
.staff-status-button__dot--break {
    background: #f97316;
}

.staff-status-option__dot--offline,
.staff-status-button__dot--offline {
    background: #94a3b8;
}

.staff-status-option__copy {
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 2px;
}

.staff-status-option__copy strong {
    color: #111827;
    font-size: 12px;
    font-weight: 800;
}

.staff-status-option__copy small {
    color: #64748b;
    font-size: 10px;
}

.staff-status-option__check {
    color: transparent;
}

.staff-status-option.is-active
.staff-status-option__check {
    color: #2563eb;
}

.staff-status-dropdown__message {
    margin: 0;
    padding: 10px 16px;
    border-top: 1px solid #fee2e2;
    background: #fef2f2;
    color: #b91c1c;
    font-size: 11px;
}

.staff-status-button[aria-expanded="true"]
.staff-status-button__arrow {
    transform: rotate(180deg);
}

.staff-status-button__arrow {
    transition: transform .16s ease;
}

@media (max-width: 640px) {
    .staff-status-dropdown {
        position: fixed;
        top: 72px;
        right: 12px;
        left: 12px;
        width: auto;
    }
}


/* ==========================================================
   Staff account menu
   ========================================================== */

.staff-account-menu {
    position: relative;
    display: inline-flex;
    align-items: center;
}

.staff-account-menu > .staff-topbar-profile {
    border: 0;
    background: transparent;
    font: inherit;
    cursor: pointer;
}

.staff-account-menu
> .staff-topbar-profile[aria-expanded="true"] {
    background: #f1f5f9 !important;
}

.staff-account-menu
> .staff-topbar-profile[aria-expanded="true"]
.staff-topbar-profile__arrow {
    transform: rotate(180deg);
}

.staff-topbar-profile__arrow {
    transition: transform .16s ease;
}

.staff-topbar-profile__avatar {
    position: relative;
}

.staff-topbar-profile__status,
.staff-account-dropdown__status {
    position: absolute;
    right: -1px;
    bottom: -1px;
    width: 10px;
    height: 10px;
    border: 2px solid #ffffff;
    border-radius: 50%;
    background: #94a3b8;
}

.staff-topbar-profile__status--online,
.staff-account-dropdown__status--online {
    background: #22c55e;
}

.staff-topbar-profile__status--working,
.staff-account-dropdown__status--working {
    background: #3b82f6;
}

.staff-topbar-profile__status--busy,
.staff-account-dropdown__status--busy {
    background: #ef4444;
}

.staff-topbar-profile__status--away,
.staff-account-dropdown__status--away {
    background: #eab308;
}

.staff-topbar-profile__status--break,
.staff-account-dropdown__status--break {
    background: #f97316;
}

.staff-topbar-profile__status--offline,
.staff-account-dropdown__status--offline {
    background: #94a3b8;
}

.staff-account-dropdown {
    position: absolute;
    top: calc(100% + 12px);
    right: 0;
    z-index: 1200;
    width: 310px;
    overflow: hidden;
    border: 1px solid #dbe3ee;
    border-radius: 17px;
    background: #ffffff;
    color: #172033;
    box-shadow:
        0 24px 70px rgba(15, 23, 42, .17),
        0 8px 24px rgba(15, 23, 42, .08);
}

.staff-account-dropdown[hidden] {
    display: none !important;
}

.staff-account-dropdown__header {
    display: flex;
    align-items: center;
    gap: 13px;
    padding: 18px;
    border-bottom: 1px solid #e5eaf1;
    background: #f8fafc;
}

.staff-account-dropdown__avatar {
    position: relative;
    width: 48px;
    height: 48px;
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 14px;
    background: #111827;
    color: #ffffff;
    font-size: 18px;
    font-weight: 800;
}

.staff-account-dropdown__identity {
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 2px;
}

.staff-account-dropdown__identity strong {
    overflow: hidden;
    color: #111827;
    font-size: 14px;
    font-weight: 800;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-account-dropdown__identity span {
    color: #475569;
    font-size: 12px;
    font-weight: 700;
}

.staff-account-dropdown__identity small {
    color: #64748b;
    font-size: 10px;
}

.staff-account-dropdown__nav {
    padding: 7px;
}

.staff-account-dropdown__nav a {
    display: flex;
    align-items: center;
    gap: 11px;
    padding: 11px 12px;
    border-radius: 10px;
    color: #334155;
    font-size: 12px;
    font-weight: 700;
    text-decoration: none;
}

.staff-account-dropdown__nav a:hover {
    background: #f1f5f9 !important;
    color: #111827 !important;
}

.staff-account-dropdown__nav svg,
.staff-account-dropdown__nav .material-symbols-rounded {
    flex: 0 0 auto;
    color: #64748b;
}

.staff-account-dropdown__divider {
    height: 1px;
    margin: 0 10px;
    background: #e5eaf1;
}

.staff-account-dropdown__logout {
    color: #dc2626 !important;
}

.staff-account-dropdown__logout svg,
.staff-account-dropdown__logout .material-symbols-rounded {
    color: #dc2626 !important;
}

.staff-account-dropdown__logout:hover {
    background: #fef2f2 !important;
    color: #b91c1c !important;
}

@media (max-width: 640px) {
    .staff-account-dropdown {
        position: fixed;
        top: 72px;
        right: 12px;
        left: 12px;
        width: auto;
    }

    .staff-topbar-profile__copy {
        display: none;
    }
}


/* ==========================================================
   Staff account menu
   ========================================================== */

.staff-account-menu {
    position: relative;
    display: inline-flex;
    align-items: center;
}

.staff-account-menu > .staff-topbar-profile {
    border: 0;
    background: transparent;
    font: inherit;
    cursor: pointer;
}

.staff-account-menu
> .staff-topbar-profile[aria-expanded="true"] {
    background: #f1f5f9 !important;
}

.staff-account-menu
> .staff-topbar-profile[aria-expanded="true"]
.staff-topbar-profile__arrow {
    transform: rotate(180deg);
}

.staff-topbar-profile__arrow {
    transition: transform .16s ease;
}

.staff-topbar-profile__avatar {
    position: relative;
}

.staff-topbar-profile__status,
.staff-account-dropdown__status {
    position: absolute;
    right: -1px;
    bottom: -1px;
    width: 10px;
    height: 10px;
    border: 2px solid #ffffff;
    border-radius: 50%;
    background: #94a3b8;
}

.staff-topbar-profile__status--online,
.staff-account-dropdown__status--online {
    background: #22c55e;
}

.staff-topbar-profile__status--working,
.staff-account-dropdown__status--working {
    background: #3b82f6;
}

.staff-topbar-profile__status--busy,
.staff-account-dropdown__status--busy {
    background: #ef4444;
}

.staff-topbar-profile__status--away,
.staff-account-dropdown__status--away {
    background: #eab308;
}

.staff-topbar-profile__status--break,
.staff-account-dropdown__status--break {
    background: #f97316;
}

.staff-topbar-profile__status--offline,
.staff-account-dropdown__status--offline {
    background: #94a3b8;
}

.staff-account-dropdown {
    position: absolute;
    top: calc(100% + 12px);
    right: 0;
    z-index: 1200;
    width: 310px;
    overflow: hidden;
    border: 1px solid #dbe3ee;
    border-radius: 17px;
    background: #ffffff;
    color: #172033;
    box-shadow:
        0 24px 70px rgba(15, 23, 42, .17),
        0 8px 24px rgba(15, 23, 42, .08);
}

.staff-account-dropdown[hidden] {
    display: none !important;
}

.staff-account-dropdown__header {
    display: flex;
    align-items: center;
    gap: 13px;
    padding: 18px;
    border-bottom: 1px solid #e5eaf1;
    background: #f8fafc;
}

.staff-account-dropdown__avatar {
    position: relative;
    width: 48px;
    height: 48px;
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 14px;
    background: #111827;
    color: #ffffff;
    font-size: 18px;
    font-weight: 800;
}

.staff-account-dropdown__identity {
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 2px;
}

.staff-account-dropdown__identity strong {
    overflow: hidden;
    color: #111827;
    font-size: 14px;
    font-weight: 800;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-account-dropdown__identity span {
    color: #475569;
    font-size: 12px;
    font-weight: 700;
}

.staff-account-dropdown__identity small {
    color: #64748b;
    font-size: 10px;
}

.staff-account-dropdown__nav {
    padding: 7px;
}

.staff-account-dropdown__nav a {
    display: flex;
    align-items: center;
    gap: 11px;
    padding: 11px 12px;
    border-radius: 10px;
    color: #334155;
    font-size: 12px;
    font-weight: 700;
    text-decoration: none;
}

.staff-account-dropdown__nav a:hover {
    background: #f1f5f9 !important;
    color: #111827 !important;
}

.staff-account-dropdown__nav svg,
.staff-account-dropdown__nav .material-symbols-rounded {
    flex: 0 0 auto;
    color: #64748b;
}

.staff-account-dropdown__divider {
    height: 1px;
    margin: 0 10px;
    background: #e5eaf1;
}

.staff-account-dropdown__logout {
    color: #dc2626 !important;
}

.staff-account-dropdown__logout svg,
.staff-account-dropdown__logout .material-symbols-rounded {
    color: #dc2626 !important;
}

.staff-account-dropdown__logout:hover {
    background: #fef2f2 !important;
    color: #b91c1c !important;
}

@media (max-width: 640px) {
    .staff-account-dropdown {
        position: fixed;
        top: 72px;
        right: 12px;
        left: 12px;
        width: auto;
    }

    .staff-topbar-profile__copy {
        display: none;
    }
}


/* ==========================================================
   Account menu open-state fix
   ========================================================== */

.staff-account-menu {
    position: relative;
    z-index: 1200;
}

.staff-account-menu > .staff-topbar-profile {
    position: relative;
    z-index: 2;
    pointer-events: auto;
}

.staff-account-dropdown {
    display: block;
    visibility: visible;
    opacity: 1;
    pointer-events: auto;
    z-index: 9999;
}

.staff-account-dropdown[hidden] {
    display: none !important;
    visibility: hidden !important;
    opacity: 0 !important;
    pointer-events: none !important;
}

.staff-account-menu.is-open
.staff-account-dropdown {
    display: block;
    visibility: visible;
    opacity: 1;
    pointer-events: auto;
}

.staff-account-menu.is-open
.staff-topbar-profile__arrow {
    transform: rotate(180deg);
}


/* Account menu definitive open fix */

[data-staff-account-menu] {
    position: relative !important;
    overflow: visible !important;
}

[data-staff-account-trigger] {
    position: relative !important;
    pointer-events: auto !important;
    cursor: pointer !important;
}

[data-staff-account-dropdown] {
    position: absolute !important;
    top: calc(100% + 12px) !important;
    right: 0 !important;
    z-index: 99999 !important;
}

[data-staff-account-dropdown][hidden] {
    display: none !important;
}

[data-staff-account-menu].is-open
[data-staff-account-dropdown] {
    display: block !important;
    visibility: visible !important;
    opacity: 1 !important;
    pointer-events: auto !important;
}


/* ==========================================================
   Staff command search
   ========================================================== */

.staff-command-search {
    width: min(430px, 42vw);
}

.staff-command-search__trigger {
    width: 100%;
    min-height: 42px;
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 0 10px 0 14px;
    border: 1px solid #dbe3ee;
    border-radius: 12px;
    background: #f8fafc;
    color: #64748b;
    font: inherit;
    cursor: pointer;
    transition:
        border-color .16s ease,
        background .16s ease,
        box-shadow .16s ease;
}

.staff-command-search__trigger:hover {
    border-color: #c7d2e2;
    background: #ffffff;
    box-shadow: 0 5px 16px rgba(15, 23, 42, .06);
}

.staff-command-search__trigger > .material-icons {
    font-size: 20px;
}

.staff-command-search__placeholder {
    min-width: 0;
    flex: 1;
    overflow: hidden;
    font-size: 12px;
    font-weight: 600;
    text-align: left;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-command-search__shortcut {
    display: inline-flex;
    align-items: center;
    gap: 3px;
}

.staff-command-search__shortcut span {
    min-width: 22px;
    height: 22px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #d7dee9;
    border-radius: 6px;
    background: #ffffff;
    color: #64748b;
    font-size: 10px;
    font-weight: 800;
    box-shadow: 0 1px 2px rgba(15, 23, 42, .05);
}

.staff-command-palette {
    position: fixed;
    inset: 0;
    z-index: 20000;
    display: flex;
    justify-content: center;
    padding: min(12vh, 110px) 20px 30px;
}

.staff-command-palette[hidden] {
    display: none !important;
}

.staff-command-palette__backdrop {
    position: absolute;
    inset: 0;
    width: 100%;
    border: 0;
    background: rgba(15, 23, 42, .48);
    backdrop-filter: blur(8px);
    cursor: default;
}

.staff-command-palette__dialog {
    position: relative;
    z-index: 1;
    width: min(680px, 100%);
    max-height: min(720px, 78vh);
    align-self: flex-start;
    overflow: hidden;
    border: 1px solid rgba(226, 232, 240, .9);
    border-radius: 20px;
    background: #ffffff;
    color: #172033;
    box-shadow:
        0 40px 100px rgba(15, 23, 42, .32),
        0 12px 36px rgba(15, 23, 42, .16);
    animation: staff-command-open .16s ease-out;
}

@keyframes staff-command-open {
    from {
        opacity: 0;
        transform: translateY(-10px) scale(.985);
    }

    to {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
}

.staff-command-palette__header {
    display: flex;
    align-items: center;
    gap: 12px;
    min-height: 66px;
    padding: 0 16px 0 20px;
    border-bottom: 1px solid #e5eaf1;
}

.staff-command-palette__header > .material-icons {
    color: #64748b;
    font-size: 24px;
}

.staff-command-palette__input {
    min-width: 0;
    flex: 1;
    border: 0;
    outline: 0;
    background: transparent;
    color: #111827;
    font: inherit;
    font-size: 15px;
    font-weight: 650;
}

.staff-command-palette__input::placeholder {
    color: #94a3b8;
}

.staff-command-palette__input::-webkit-search-cancel-button {
    display: none;
}

.staff-command-palette__escape {
    padding: 6px 8px;
    border: 1px solid #dbe3ee;
    border-radius: 7px;
    background: #f8fafc;
    color: #64748b;
    font-size: 9px;
    font-weight: 800;
    cursor: pointer;
}

.staff-command-palette__body {
    max-height: calc(min(720px, 78vh) - 116px);
    overflow-y: auto;
    padding: 10px;
}

.staff-command-section + .staff-command-section {
    margin-top: 10px;
}

.staff-command-section__heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 8px 10px 7px;
    color: #64748b;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: .08em;
    text-transform: uppercase;
}

.staff-command-section__heading button {
    border: 0;
    background: transparent;
    color: #64748b;
    font: inherit;
    font-size: 9px;
    cursor: pointer;
    text-transform: none;
}

.staff-command-section__heading button:hover {
    color: #2563eb;
}

.staff-command-results {
    display: grid;
    gap: 3px;
}

.staff-command-result {
    display: flex;
    align-items: center;
    gap: 12px;
    min-height: 58px;
    padding: 9px 12px;
    border: 1px solid transparent;
    border-radius: 12px;
    color: #334155;
    text-decoration: none;
    transition:
        border-color .12s ease,
        background .12s ease,
        transform .12s ease;
}

.staff-command-result:hover,
.staff-command-result.is-active {
    border-color: #dbeafe;
    background: #eff6ff;
    color: #172033;
    transform: translateX(2px);
}

.staff-command-result__icon {
    width: 38px;
    height: 38px;
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 10px;
    background: #f1f5f9;
    color: #475569;
    font-size: 20px;
}

.staff-command-result:hover
.staff-command-result__icon,
.staff-command-result.is-active
.staff-command-result__icon {
    background: #dbeafe;
    color: #2563eb;
}

.staff-command-result__copy {
    min-width: 0;
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 3px;
}

.staff-command-result__copy strong {
    overflow: hidden;
    color: #172033;
    font-size: 12px;
    font-weight: 800;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-command-result__copy small {
    overflow: hidden;
    color: #64748b;
    font-size: 10px;
    font-weight: 550;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-command-result__arrow,
.staff-command-result__recent {
    flex: 0 0 auto;
    color: #94a3b8;
    font-size: 18px;
}

.staff-command-empty {
    padding: 48px 20px;
    text-align: center;
}

.staff-command-empty > .material-icons {
    display: block;
    margin-bottom: 12px;
    color: #94a3b8;
    font-size: 38px;
}

.staff-command-empty strong {
    display: block;
    color: #334155;
    font-size: 13px;
}

.staff-command-empty p {
    margin: 5px 0 0;
    color: #94a3b8;
    font-size: 11px;
}

.staff-command-palette__footer {
    min-height: 50px;
    display: flex;
    align-items: center;
    gap: 18px;
    padding: 0 18px;
    border-top: 1px solid #e5eaf1;
    background: #f8fafc;
    color: #64748b;
    font-size: 9px;
    font-weight: 700;
}

.staff-command-palette__footer span {
    display: inline-flex;
    align-items: center;
    gap: 5px;
}

.staff-command-palette__footer kbd {
    min-width: 21px;
    height: 20px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #d7dee9;
    border-bottom-width: 2px;
    border-radius: 5px;
    background: #ffffff;
    color: #475569;
    font-family: inherit;
    font-size: 9px;
}

body.staff-command-palette-open {
    overflow: hidden;
}

@media (max-width: 780px) {
    .staff-command-search {
        width: auto;
        flex: 1;
    }

    .staff-command-search__placeholder {
        display: none;
    }

    .staff-command-search__trigger {
        width: 42px;
        padding: 0;
        justify-content: center;
    }

    .staff-command-search__shortcut {
        display: none;
    }

    .staff-command-palette {
        padding: 12px;
    }

    .staff-command-palette__dialog {
        width: 100%;
        max-height: calc(100vh - 24px);
        border-radius: 16px;
    }

    .staff-command-palette__body {
        max-height: calc(100vh - 140px);
    }

    .staff-command-palette__footer {
        gap: 10px;
        overflow-x: auto;
    }
}


/* ==========================================================
   Staff command search responsive sizing
   ========================================================== */

.staff-command-search {
    width: clamp(180px, 28vw, 430px);
    min-width: 0;
    flex: 0 1 430px;
}

.staff-command-search__trigger {
    min-width: 0;
}

.staff-command-search__placeholder {
    min-width: 0;
}

.staff-command-search__shortcut {
    flex: 0 0 auto;
}

.staff-command-search__shortcut span:first-child {
    min-width: auto;
    padding-inline: 6px;
}

/*
 * ヘッダー内の検索欄が、通知やプロフィールを
 * 押し出さないようにする。
 */
.staff-topbar {
    min-width: 0;
}

.staff-topbar > * {
    min-width: 0;
}

.staff-topbar__actions,
.staff-topbar-actions {
    flex: 0 0 auto;
}

@media (max-width: 1200px) {
    .staff-command-search {
        width: clamp(160px, 23vw, 320px);
        flex-basis: 320px;
    }

    .staff-command-search__placeholder {
        font-size: 11px;
    }
}

@media (max-width: 1024px) {
    .staff-command-search {
        width: clamp(140px, 20vw, 250px);
        flex-basis: 250px;
    }

    .staff-command-search__shortcut {
        display: none;
    }
}

@media (max-width: 860px) {
    .staff-command-search {
        width: 42px;
        min-width: 42px;
        max-width: 42px;
        flex: 0 0 42px;
    }

    .staff-command-search__trigger {
        width: 42px;
        min-width: 42px;
        padding: 0;
        justify-content: center;
    }

    .staff-command-search__placeholder,
    .staff-command-search__shortcut {
        display: none;
    }
}

@media (max-width: 640px) {
    .staff-command-search {
        width: 38px;
        min-width: 38px;
        max-width: 38px;
        flex-basis: 38px;
    }

    .staff-command-search__trigger {
        width: 38px;
        min-width: 38px;
        min-height: 38px;
        border-radius: 10px;
    }
}


/* ==========================================================
   Staff search mobile optimization
   ========================================================== */

.staff-command-search {
    min-width: 0;
}

.staff-command-search__trigger {
    min-width: 0;
}

.staff-command-search__placeholder {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

/*
 * デスクトップ
 */
@media (min-width: 1201px) {
    .staff-command-search {
        width: clamp(220px, 30vw, 430px);
        max-width: 430px;
        flex: 0 1 430px;
    }
}

/*
 * 小さめデスクトップ・タブレット横
 */
@media (max-width: 1200px) {
    .staff-command-search {
        width: clamp(180px, 26vw, 320px);
        max-width: 320px;
        flex: 0 1 320px;
    }
}

@media (max-width: 1024px) {
    .staff-command-search {
        width: clamp(150px, 22vw, 250px);
        max-width: 250px;
        flex: 0 1 250px;
    }

    .staff-command-search__shortcut {
        display: none;
    }

    .staff-command-search__placeholder {
        font-size: 10px;
    }
}

/*
 * タブレット縦・スマホ
 * 検索欄をアイコンボタンへ縮小
 */
@media (max-width: 820px) {
    .staff-command-search {
        width: 42px;
        min-width: 42px;
        max-width: 42px;
        flex: 0 0 42px;
    }

    .staff-command-search__trigger {
        width: 42px;
        min-width: 42px;
        height: 42px;
        min-height: 42px;
        padding: 0;
        justify-content: center;
        border-radius: 11px;
    }

    .staff-command-search__placeholder,
    .staff-command-search__shortcut {
        display: none;
    }

    .staff-command-search__trigger > .material-icons {
        font-size: 21px;
    }
}

/*
 * スマホ用検索パレット
 */
@media (max-width: 640px) {
    .staff-command-search {
        width: 38px;
        min-width: 38px;
        max-width: 38px;
        flex-basis: 38px;
    }

    .staff-command-search__trigger {
        width: 38px;
        min-width: 38px;
        height: 38px;
        min-height: 38px;
        border-radius: 10px;
    }

    .staff-command-palette {
        align-items: stretch;
        justify-content: stretch;
        padding: 0;
    }

    .staff-command-palette__dialog {
        width: 100%;
        max-width: none;
        height: 100%;
        max-height: none;
        align-self: stretch;
        border: 0;
        border-radius: 0;
        animation: staff-command-mobile-open .18s ease-out;
    }

    @keyframes staff-command-mobile-open {
        from {
            opacity: 0;
            transform: translateY(16px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .staff-command-palette__header {
        min-height: 64px;
        padding:
            calc(10px + env(safe-area-inset-top))
            12px
            10px
            16px;
    }

    .staff-command-palette__input {
        font-size: 16px;
    }

    .staff-command-palette__escape {
        min-width: 42px;
        min-height: 34px;
        padding: 0 8px;
    }

    .staff-command-palette__body {
        max-height: none;
        height: calc(
            100vh
            - 64px
            - 50px
            - env(safe-area-inset-top)
            - env(safe-area-inset-bottom)
        );
        padding: 8px;
        overscroll-behavior: contain;
    }

    .staff-command-result {
        min-height: 64px;
        padding: 10px;
        border-radius: 12px;
    }

    .staff-command-result__icon {
        width: 42px;
        height: 42px;
        border-radius: 11px;
        font-size: 21px;
    }

    .staff-command-result__copy strong {
        font-size: 13px;
    }

    .staff-command-result__copy small {
        font-size: 11px;
    }

    .staff-command-palette__footer {
        min-height: calc(
            50px + env(safe-area-inset-bottom)
        );
        padding:
            0
            12px
            env(safe-area-inset-bottom);
        gap: 12px;
    }

    .staff-command-palette__footer span:nth-child(2) {
        display: none;
    }
}

/*
 * かなり狭い画面
 */
@media (max-width: 390px) {
    .staff-command-palette__header {
        gap: 8px;
        padding-left: 12px;
    }

    .staff-command-palette__header > .material-icons {
        font-size: 21px;
    }

    .staff-command-palette__escape {
        min-width: 38px;
        font-size: 8px;
    }

    .staff-command-palette__footer {
        justify-content: space-between;
        font-size: 8px;
    }
}


/* ==========================================================
   Staff search definitive overlay fix
   ========================================================== */

.staff-command-palette[hidden] {
    display: none !important;
}

.staff-command-palette {
    position: fixed !important;
    inset: 0 !important;
    z-index: 100000 !important;

    display: flex;
    align-items: flex-start;
    justify-content: center;

    width: 100vw !important;
    height: 100dvh !important;
    margin: 0 !important;
    padding: 72px 20px 20px;

    background: rgba(15, 23, 42, 0.52);
    backdrop-filter: blur(8px);
}

html.staff-search-is-open,
body.staff-search-is-open {
    overflow: hidden !important;
}

@media (max-width: 640px) {
    .staff-command-palette {
        align-items: stretch !important;
        justify-content: stretch !important;

        width: 100vw !important;
        height: 100dvh !important;
        min-height: 100dvh !important;
        padding: 0 !important;

        background: #ffffff !important;
    }

    .staff-command-palette__dialog {
        position: relative !important;

        width: 100% !important;
        max-width: none !important;
        height: 100% !important;
        min-height: 100dvh !important;
        max-height: none !important;
        margin: 0 !important;

        border: 0 !important;
        border-radius: 0 !important;
        box-shadow: none !important;
    }

    .staff-command-palette__body {
        flex: 1 1 auto;
        height: auto !important;
        max-height: none !important;
        overflow-y: auto;
        -webkit-overflow-scrolling: touch;
    }
}


/* ==========================================================
   Staff command search final responsive fix
   ========================================================== */

.staff-command-search {
    width: clamp(210px, 28vw, 430px);
    min-width: 0;
    max-width: 430px;
    flex: 0 1 430px;
}

.staff-command-search__trigger {
    display: flex;
    width: 100%;
    height: 42px;
    min-width: 0;
    align-items: center;
    gap: 10px;
    padding: 0 11px;

    border: 1px solid #d8e1ec;
    border-radius: 11px;
    color: #475569;
    background: #f8fafc;

    cursor: pointer;
}

.staff-command-search__trigger:hover {
    border-color: #93c5fd;
    background: #eff6ff;
}

.staff-command-search__placeholder {
    min-width: 0;
    flex: 1;
    overflow: hidden;

    color: #64748b;
    text-align: left;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.staff-command-search__shortcut {
    display: inline-flex;
    flex: 0 0 auto;
    align-items: center;
    gap: 3px;
}

.staff-command-search__shortcut > span {
    display: inline-flex;
    min-height: 22px;
    align-items: center;
    justify-content: center;
    padding: 0 6px;

    border: 1px solid #d8e1ec;
    border-radius: 6px;
    background: #ffffff;

    font-size: 10px;
}

.staff-command-palette[hidden] {
    display: none !important;
}

.staff-command-palette {
    position: fixed !important;
    inset: 0 !important;
    z-index: 100000 !important;

    display: flex !important;
    width: 100vw !important;
    height: 100dvh !important;
    align-items: flex-start;
    justify-content: center;
    margin: 0 !important;
    padding: 72px 20px 20px !important;

    background: rgba(15, 23, 42, 0.56);
    backdrop-filter: blur(8px);
}

.staff-command-palette__backdrop {
    position: absolute !important;
    inset: 0 !important;
    z-index: 0 !important;

    width: 100% !important;
    height: 100% !important;
    padding: 0 !important;

    border: 0 !important;
    background: transparent !important;
}

.staff-command-palette__dialog {
    position: relative !important;
    z-index: 1 !important;

    display: flex;
    width: min(720px, 100%) !important;
    max-height: calc(100dvh - 100px);
    flex-direction: column;
    overflow: hidden;

    border: 1px solid #d8e1ec;
    border-radius: 18px;
    background: #ffffff;
    box-shadow:
        0 30px 100px rgba(15, 23, 42, 0.28),
        0 10px 35px rgba(15, 23, 42, 0.14);
}

html.staff-command-palette-open,
body.staff-command-palette-open {
    overflow: hidden !important;
}

@media (max-width: 1200px) {
    .staff-command-search {
        width: clamp(170px, 23vw, 320px);
        flex-basis: 320px;
    }
}

@media (max-width: 1024px) {
    .staff-command-search {
        width: clamp(145px, 20vw, 250px);
        flex-basis: 250px;
    }

    .staff-command-search__shortcut {
        display: none;
    }
}

@media (max-width: 820px) {
    .staff-command-search {
        width: 42px;
        min-width: 42px;
        max-width: 42px;
        flex: 0 0 42px;
    }

    .staff-command-search__trigger {
        width: 42px;
        height: 42px;
        justify-content: center;
        padding: 0;
    }

    .staff-command-search__placeholder,
    .staff-command-search__shortcut {
        display: none;
    }
}

@media (max-width: 640px) {
    .staff-command-search {
        width: 38px;
        min-width: 38px;
        max-width: 38px;
        flex-basis: 38px;
    }

    .staff-command-search__trigger {
        width: 38px;
        height: 38px;
        border-radius: 10px;
    }

    .staff-command-palette {
        align-items: stretch !important;
        justify-content: stretch !important;
        padding: 0 !important;
        background: #ffffff !important;
    }

    .staff-command-palette__dialog {
        width: 100% !important;
        max-width: none !important;
        height: 100dvh !important;
        min-height: 100dvh !important;
        max-height: none !important;
        margin: 0 !important;

        border: 0 !important;
        border-radius: 0 !important;
        box-shadow: none !important;
    }

    .staff-command-palette__header {
        flex: 0 0 auto;
        padding-top: max(
            12px,
            env(safe-area-inset-top)
        );
    }

    .staff-command-palette__input {
        font-size: 16px !important;
    }

    .staff-command-palette__body {
        flex: 1 1 auto;
        height: auto !important;
        max-height: none !important;
        overflow-y: auto !important;
        overscroll-behavior: contain;
        -webkit-overflow-scrolling: touch;
    }

    .staff-command-palette__footer {
        flex: 0 0 auto;
        padding-bottom: max(
            10px,
            env(safe-area-inset-bottom)
        );
    }
}


==================================================
staff/staff.js
==================================================
(() => {
    'use strict';

    const sidebar = document.querySelector(
        '[data-staff-sidebar]'
    );

    const backdrop = document.querySelector(
        '[data-staff-sidebar-backdrop]'
    );

    const openButton = document.querySelector(
        '[data-staff-sidebar-open]'
    );

    const closeButton = document.querySelector(
        '[data-staff-sidebar-close]'
    );

    const openSidebar = () => {
        sidebar?.classList.add(
            'staff-sidebar--open'
        );

        backdrop?.classList.add(
            'staff-sidebar-backdrop--visible'
        );

        document.body.style.overflow = 'hidden';
    };

    const closeSidebar = () => {
        sidebar?.classList.remove(
            'staff-sidebar--open'
        );

        backdrop?.classList.remove(
            'staff-sidebar-backdrop--visible'
        );

        document.body.style.overflow = '';
    };

    openButton?.addEventListener(
        'click',
        openSidebar
    );

    closeButton?.addEventListener(
        'click',
        closeSidebar
    );

    backdrop?.addEventListener(
        'click',
        closeSidebar
    );

    window.addEventListener(
        'keydown',
        (event) => {
            if (event.key === 'Escape') {
                closeSidebar();
            }
        }
    );

    window.addEventListener(
        'resize',
        () => {
            if (window.innerWidth > 800) {
                closeSidebar();
            }
        }
    );
})();

/* ==========================================================
   Staff Notification Center
   ========================================================== */

(() => {
    'use strict';

    const root = document.querySelector('[data-staff-notifications]');

    if (!root) {
        return;
    }

    const trigger = root.querySelector('[data-notification-trigger]');
    const panel = root.querySelector('[data-notification-panel]');
    const badge = root.querySelector('[data-notification-badge]');
    const body = root.querySelector('[data-notification-body]');
    const readAllButton = root.querySelector(
        '[data-notification-read-all]'
    );
    const footerCount = root.querySelector(
        '[data-notification-footer-count]'
    );
    const categoryButtons = Array.from(
        root.querySelectorAll('[data-notification-category]')
    );

    let activeCategory = 'all';
    let notifications = [];
    let loading = false;

    const categoryLabels = {
        system: 'システム',
        order: '注文',
        user: 'ユーザー',
        discord: 'Discord',
        github: 'GitHub',
        development: '開発'
    };

    const levelSymbols = {
        critical: '!',
        warning: '!',
        success: '✓',
        info: 'i',
        development: '</>'
    };

    const escapeHtml = (value) => {
        const div = document.createElement('div');
        div.textContent = String(value ?? '');
        return div.innerHTML;
    };

    const formatDate = (value) => {
        if (!value) {
            return '';
        }

        const normalized = String(value).replace(
            ' ',
            'T'
        );

        const date = new Date(normalized);

        if (Number.isNaN(date.getTime())) {
            return String(value);
        }

        const seconds = Math.floor(
            (Date.now() - date.getTime()) / 1000
        );

        if (seconds < 60) {
            return 'たった今';
        }

        const minutes = Math.floor(seconds / 60);

        if (minutes < 60) {
            return `${minutes}分前`;
        }

        const hours = Math.floor(minutes / 60);

        if (hours < 24) {
            return `${hours}時間前`;
        }

        const days = Math.floor(hours / 24);

        if (days < 7) {
            return `${days}日前`;
        }

        return new Intl.DateTimeFormat('ja-JP', {
            month: 'short',
            day: 'numeric',
            hour: '2-digit',
            minute: '2-digit'
        }).format(date);
    };

    const updateBadge = (count) => {
        const unread = Number(count) || 0;

        if (unread <= 0) {
            badge.hidden = true;
            badge.textContent = '0';
            return;
        }

        badge.hidden = false;
        badge.textContent = unread > 99
            ? '99+'
            : String(unread);
    };

    const notificationItemHtml = (item) => {
        const level = [
            'critical',
            'warning',
            'success',
            'info',
            'development'
        ].includes(item.level)
            ? item.level
            : 'info';

        const symbol = levelSymbols[level] || 'i';
        const category = categoryLabels[item.category]
            || item.category
            || '通知';

        return `
            <button
                type="button"
                class="staff-notifications__item${
                    item.is_read ? '' : ' is-unread'
                }"
                data-notification-id="${Number(item.id)}"
                data-notification-url="${
                    escapeHtml(item.url || '')
                }"
            >
                <span
                    class="staff-notifications__icon
                    staff-notifications__icon--${level}"
                    aria-hidden="true"
                >
                    ${escapeHtml(symbol)}
                </span>

                <span class="staff-notifications__content">
                    <strong class="staff-notifications__item-title">
                        ${escapeHtml(item.title)}
                    </strong>

                    <span class="staff-notifications__message">
                        ${escapeHtml(item.message || '')}
                    </span>

                    <span class="staff-notifications__meta">
                        <span class="staff-notifications__category">
                            ${escapeHtml(category)}
                        </span>

                        <time>
                            ${escapeHtml(formatDate(item.created_at))}
                        </time>

                        ${
                            item.url
                                ? `
                                    <span
                                        class="staff-notifications__open"
                                        data-notification-open
                                        role="link"
                                        tabindex="0"
                                    >
                                        開く
                                        <span aria-hidden="true">→</span>
                                    </span>
                                `
                                : ''
                        }
                    </span>
                </span>
            </button>
        `;
    };

    const render = () => {
        if (!notifications.length) {
            body.innerHTML = `
                <div class="staff-notifications__empty">
                    現在表示できる通知はありません。
                </div>
            `;

            footerCount.textContent = '通知はありません';
            readAllButton.disabled = true;
            return;
        }

        body.innerHTML = notifications
            .map(notificationItemHtml)
            .join('');

        const unreadCount = notifications.filter(
            (item) => !item.is_read
        ).length;

        footerCount.textContent =
            `${notifications.length}件を表示中`;

        readAllButton.disabled = unreadCount === 0;
    };

    const loadNotifications = async ({
        silent = false
    } = {}) => {
        if (loading) {
            return;
        }

        loading = true;

        if (!silent) {
            body.innerHTML = `
                <div class="staff-notifications__loading">
                    通知を読み込んでいます
                </div>
            `;
        }

        try {
            const response = await fetch(
                `/staff/api/notifications/?category=${
                    encodeURIComponent(activeCategory)
                }&limit=30`,
                {
                    credentials: 'same-origin',
                    headers: {
                        Accept: 'application/json'
                    },
                    cache: 'no-store'
                }
            );

            const data = await response.json();

            if (!response.ok || !data.ok) {
                throw new Error(
                    data.message || '通知の取得に失敗しました。'
                );
            }

            notifications = Array.isArray(data.items)
                ? data.items
                : [];

            updateBadge(data.unread);
            render();
        } catch (error) {
            if (!silent) {
                body.innerHTML = `
                    <div class="staff-notifications__error">
                        ${escapeHtml(
                            error.message
                            || '通知の取得に失敗しました。'
                        )}
                    </div>
                `;

                footerCount.textContent =
                    '通知を取得できませんでした';
            }
        } finally {
            loading = false;
        }
    };

    const markAsRead = async (id) => {
        const response = await fetch(
            '/staff/api/notifications/read.php',
            {
                method: 'POST',
                credentials: 'same-origin',
                headers: {
                    'Content-Type': 'application/json',
                    Accept: 'application/json'
                },
                body: JSON.stringify({
                    id
                })
            }
        );

        const data = await response.json();

        if (!response.ok || !data.ok) {
            throw new Error(
                data.message || '既読処理に失敗しました。'
            );
        }

        const item = notifications.find(
            (notification) => Number(notification.id) === Number(id)
        );

        if (item) {
            item.is_read = true;
        }

        return data;
    };

    const markAllAsRead = async () => {
        readAllButton.disabled = true;

        try {
            const response = await fetch(
                '/staff/api/notifications/read-all.php',
                {
                    method: 'POST',
                    credentials: 'same-origin',
                    headers: {
                        Accept: 'application/json'
                    }
                }
            );

            const data = await response.json();

            if (!response.ok || !data.ok) {
                throw new Error(
                    data.message
                    || '一括既読処理に失敗しました。'
                );
            }

            notifications = notifications.map((item) => ({
                ...item,
                is_read: true
            }));

            updateBadge(0);
            render();
        } catch (error) {
            console.error(error);
            readAllButton.disabled = false;
        }
    };

    const openPanel = () => {
        panel.hidden = false;
        trigger.setAttribute('aria-expanded', 'true');
        loadNotifications();
    };

    const closePanel = () => {
        panel.hidden = true;
        trigger.setAttribute('aria-expanded', 'false');
    };

    trigger.addEventListener('click', (event) => {
        event.stopPropagation();

        if (panel.hidden) {
            openPanel();
        } else {
            closePanel();
        }
    });

    categoryButtons.forEach((button) => {
        button.addEventListener('click', () => {
            activeCategory =
                button.dataset.notificationCategory || 'all';

            categoryButtons.forEach((item) => {
                item.classList.toggle(
                    'is-active',
                    item === button
                );
            });

            loadNotifications();
        });
    });

    readAllButton.addEventListener('click', () => {
        markAllAsRead();
    });

    body.addEventListener('click', async (event) => {
        const item = event.target.closest(
            '[data-notification-id]'
        );

        if (!item) {
            return;
        }

        const openButton = event.target.closest(
            '[data-notification-open]'
        );

        const url = item.dataset.notificationUrl || '';

        if (openButton) {
            event.preventDefault();
            event.stopPropagation();

            if (url) {
                window.location.href = url;
            }

            return;
        }

        event.preventDefault();

        const id = Number(item.dataset.notificationId);

        if (
            !id
            || !item.classList.contains('is-unread')
            || item.dataset.notificationReading === 'true'
        ) {
            return;
        }

        item.dataset.notificationReading = 'true';

        try {
            await markAsRead(id);

            item.classList.remove('is-unread');

            const notification = notifications.find(
                (entry) => Number(entry.id) === id
            );

            if (notification) {
                notification.is_read = true;
            }

            const unreadCount = notifications.filter(
                (entry) => !entry.is_read
            ).length;

            updateBadge(unreadCount);
            readAllButton.disabled = unreadCount === 0;
        } catch (error) {
            console.error(error);
        } finally {
            delete item.dataset.notificationReading;
        }
    });

    body.addEventListener('keydown', (event) => {
        const openButton = event.target.closest(
            '[data-notification-open]'
        );

        if (
            !openButton
            || (
                event.key !== 'Enter'
                && event.key !== ' '
            )
        ) {
            return;
        }

        event.preventDefault();
        event.stopPropagation();

        const item = openButton.closest(
            '[data-notification-id]'
        );

        const url = item?.dataset.notificationUrl || '';

        if (url) {
            window.location.href = url;
        }
    });

    document.addEventListener('click', (event) => {
        if (!root.contains(event.target)) {
            closePanel();
        }
    });

    document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape') {
            closePanel();
            trigger.focus();
        }
    });

    loadNotifications({
        silent: true
    });

    window.setInterval(() => {
        loadNotifications({
            silent: true
        });
    }, 30000);
})();

/* ==========================================================
   Staff work status menu
   ========================================================== */

(() => {
    'use strict';

    const root = document.querySelector(
        '[data-staff-status-menu]'
    );

    if (!root) {
        return;
    }

    const trigger = root.querySelector(
        '[data-staff-status-trigger]'
    );
    const dropdown = root.querySelector(
        '[data-staff-status-dropdown]'
    );
    const label = root.querySelector(
        '[data-staff-status-label]'
    );
    const dot = root.querySelector(
        '[data-staff-status-dot]'
    );
    const message = root.querySelector(
        '[data-staff-status-message]'
    );
    const options = Array.from(
        root.querySelectorAll(
            '[data-staff-status-option]'
        )
    );

    if (
        !trigger
        || !dropdown
        || !label
        || !dot
        || !message
    ) {
        return;
    }

    const allowedStatuses = [
        'online',
        'working',
        'busy',
        'away',
        'break',
        'offline'
    ];

    const close = () => {
        dropdown.hidden = true;
        trigger.setAttribute(
            'aria-expanded',
            'false'
        );
    };

    const open = () => {
        dropdown.hidden = false;
        trigger.setAttribute(
            'aria-expanded',
            'true'
        );
        message.hidden = true;
    };

    const setLoading = (loading) => {
        options.forEach((option) => {
            option.disabled = loading;
        });
    };

    const updateDisplay = (status, statusLabel) => {
        label.textContent = statusLabel;
        trigger.title = statusLabel;

        allowedStatuses.forEach((value) => {
            dot.classList.remove(
                `staff-status-button__dot--${value}`
            );
        });

        dot.classList.add(
            `staff-status-button__dot--${status}`
        );

        options.forEach((option) => {
            option.classList.toggle(
                'is-active',
                option.dataset.staffStatusOption
                    === status
            );
        });

        const accountDots = document.querySelectorAll(
            '[data-staff-account-status-dot], '
            + '[data-staff-account-dropdown-status]'
        );

        accountDots.forEach((accountDot) => {
            allowedStatuses.forEach((value) => {
                accountDot.classList.remove(
                    `staff-topbar-profile__status--${value}`,
                    `staff-account-dropdown__status--${value}`
                );
            });

            if (
                accountDot.hasAttribute(
                    'data-staff-account-status-dot'
                )
            ) {
                accountDot.classList.add(
                    `staff-topbar-profile__status--${status}`
                );
            } else {
                accountDot.classList.add(
                    `staff-account-dropdown__status--${status}`
                );
            }
        });

        const accountStatusLabel = document.querySelector(
            '[data-staff-account-status-label]'
        );

        if (accountStatusLabel) {
            accountStatusLabel.textContent = statusLabel;
        }
    };

    const updateStatus = async (status) => {
        setLoading(true);
        message.hidden = true;

        try {
            const response = await fetch(
                '/staff/api/status/update.php',
                {
                    method: 'POST',
                    credentials: 'same-origin',
                    headers: {
                        'Content-Type': 'application/json',
                        Accept: 'application/json'
                    },
                    body: JSON.stringify({
                        status
                    })
                }
            );

            const data = await response.json();

            if (!response.ok || !data.ok) {
                throw new Error(
                    data.message
                    || '勤務状態を更新できませんでした。'
                );
            }

            updateDisplay(
                data.status,
                data.label
            );

            close();
        } catch (error) {
            message.textContent =
                error.message
                || '勤務状態を更新できませんでした。';

            message.hidden = false;
        } finally {
            setLoading(false);
        }
    };

    trigger.addEventListener('click', (event) => {
        event.stopPropagation();

        if (dropdown.hidden) {
            open();
        } else {
            close();
        }
    });

    options.forEach((option) => {
        option.addEventListener('click', () => {
            const status =
                option.dataset.staffStatusOption;

            if (
                !status
                || !allowedStatuses.includes(status)
                || option.classList.contains('is-active')
            ) {
                close();
                return;
            }

            updateStatus(status);
        });
    });

    document.addEventListener('click', (event) => {
        if (!root.contains(event.target)) {
            close();
        }
    });

    document.addEventListener('keydown', (event) => {
        if (
            event.key === 'Escape'
            && !dropdown.hidden
        ) {
            close();
            trigger.focus();
        }
    });
})();

/* ==========================================================
   Staff account menu
   ========================================================== */

(() => {
    'use strict';

    const root = document.querySelector(
        '[data-staff-account-menu]'
    );

    if (!root) {
        return;
    }

    const trigger = root.querySelector(
        '[data-staff-account-trigger]'
    );
    const dropdown = root.querySelector(
        '[data-staff-account-dropdown]'
    );

    if (!trigger || !dropdown) {
        return;
    }

    const close = () => {
        dropdown.hidden = true;
        trigger.setAttribute(
            'aria-expanded',
            'false'
        );
    };

    const open = () => {
        dropdown.hidden = false;
        trigger.setAttribute(
            'aria-expanded',
            'true'
        );
    };

    trigger.addEventListener('click', (event) => {
        event.stopPropagation();

        if (dropdown.hidden) {
            open();
        } else {
            close();
        }
    });

    document.addEventListener('click', (event) => {
        if (!root.contains(event.target)) {
            close();
        }
    });

    document.addEventListener('keydown', (event) => {
        if (
            event.key === 'Escape'
            && !dropdown.hidden
        ) {
            close();
            trigger.focus();
        }
    });
})();

/* ==========================================================
   Staff account menu
   ========================================================== */

(() => {
    'use strict';

    const root = document.querySelector(
        '[data-staff-account-menu]'
    );

    if (!root) {
        return;
    }

    const trigger = root.querySelector(
        '[data-staff-account-trigger]'
    );
    const dropdown = root.querySelector(
        '[data-staff-account-dropdown]'
    );

    if (!trigger || !dropdown) {
        return;
    }

    const close = () => {
        dropdown.hidden = true;
        trigger.setAttribute(
            'aria-expanded',
            'false'
        );
    };

    const open = () => {
        dropdown.hidden = false;
        trigger.setAttribute(
            'aria-expanded',
            'true'
        );
    };

    trigger.addEventListener('click', (event) => {
        event.stopPropagation();

        if (dropdown.hidden) {
            open();
        } else {
            close();
        }
    });

    document.addEventListener('click', (event) => {
        if (!root.contains(event.target)) {
            close();
        }
    });

    document.addEventListener('keydown', (event) => {
        if (
            event.key === 'Escape'
            && !dropdown.hidden
        ) {
            close();
            trigger.focus();
        }
    });
})();

==================================================
STAFF DIRECTORY TREE
==================================================
staff/.DS_Store
staff/account-menu.js
staff/api/notifications/index.php
staff/api/notifications/read-all.php
staff/api/notifications/read.php
staff/api/status/update.php
staff/components/layout.php
staff/components/notifications-panel.php
staff/components/notifications.php
staff/components/search-palette.php
staff/components/ui.php
staff/index.php
staff/lib/auth.php
staff/lib/bootstrap.php
staff/lib/dashboard.php
staff/lib/db.php
staff/lib/icon.php
staff/lib/navigation.php
staff/lib/permissions.php
staff/lib/staff.php
staff/parts/sidebar.php
staff/parts/topbar.php
staff/search-palette.js
staff/staff.css
staff/staff.js
staff/tasks/index.php
